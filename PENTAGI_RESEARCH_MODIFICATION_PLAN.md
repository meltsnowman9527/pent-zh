# PentAGI 按研究报告架构改造计划

## 最新部署与进度基线（2026-09-15）

用户取消保留原版和独立开发部署的要求。后续直接修改当前源码，只更新原 `pentagi` 应用和原 Compose 项目，统一使用 https://localhost:8443；数据库等原有必要配套服务继续保留。不再新增第二套应用部署，不再制作备份。Docker 更新可以重建同名应用容器，不再要求旧容器编号或镜像固定。

此前的开发副本容器、网络、七个运行数据卷及镜像备份标签已撤掉；完整备份归档和旧模型配置备份已按用户要求删除。下文关于冻结原版、独立开发入口和备份恢复的条目仅保留为历史记录，不再执行。单套更新说明见 `DEPLOYMENT.md`。

P0/P0A 已基本完成并部署：生命周期同步已按 `flow_id` 隔离，后台执行器最多并发处理 4 个不同流程且保持单流程 FIFO；中文化、PDF 表格与跨页表头、密码可见性按钮键盘焦点已完成自动化及浏览器级核验。当前入口为 https://localhost:8443。尚未完成 NVDA/讲述人真实语音走查；P1 至 P6 业务能力尚未开始。

本计划依据《中期研究报告0830.docx》第3章总体架构、第4至6章关键技术、第7章系统设计及第8章实验方案，并对照当前工作区代码。目标是在现有 PentAGI 上建设面向授权测评的资产、漏洞、候选利用链、受控验证与证据管理平台。本轮仅制定计划，不修改程序、数据库或运行配置。

按用户最新要求，报告约束主要技术路线，不要求逐项复制全部架构、智能体和管理页面。优先复用现有智能体与 Flow 执行能力，按实际缺口增加角色；前端采用多页面组织技术环节。前端全部静态界面文字改为简体中文属于明确交付范围，覆盖现有页面与新增页面。

改造同时保留并完善 PentAGI 中优于报告细化程度或报告未充分覆盖的现有能力。报告提供研究主线，现有项目提供可用的工程基础；不能因报告未列出某项能力而删除它，也不以增加模块或智能体数量代替使用体验、稳定性和结果质量的改善。

## 当前确认的首版目标

报告的研究主线是“扫描 → 收集 → 推理 → 利用”四个技术环节；系统中的“报告输出”是交付与证据汇总阶段，不另算研究环节。首版界面仍按“资产发现/漏洞扫描 → 漏洞收集 → 利用链推理 → 渗透测试 → 报告输出”五阶段呈现，但数据状态、实验统计和论文表述均按四环节主线加交付阶段解释，避免口径冲突。以下首版范围优先于后文完整能力清单；P1 至 P6 中的扩展项不再全部作为首版前置条件。

界面采用多页面结构，四个研究环节与报告交付分别提供独立工作页面，由任务中心贯通，不把全部操作集中在 Flow 内。Flow 复用为执行详情、实时消息、工具轨迹和助手交互入口。各页面展示相应输入、当前状态、结果、证据及下一步。阶段完成由实际产物和必要校验决定，不能只显示模型生成的完成文字。已有资料可以导入并复用，但要标记来源与本次是否执行，不能显示为本次扫描所得。

### 首版页面组织

| 页面 | 主要内容与操作 |
|---|---|
| 任务中心 | 创建任务、配置范围、查看五阶段进度、进入各阶段及执行详情 |
| 资产发现与漏洞扫描 | 资产与服务清单、发现/扫描操作、扫描批次、结果和原始证据 |
| 漏洞收集 | 情报检索与导入、来源、影响范围、去重结果及资产漏洞关联 |
| 利用链推理 | 发起分析、路径列表与关系图、节点条件、证据、待确认项及提交验证 |
| 渗透测试 | 验证计划、执行控制、实时活动、助手介入、结果与证据 |
| 报告输出 | 选择任务及结果、生成中文报告、查看引用证据与导出 |

侧栏按技术路线排列页面，各页保留当前任务选择和任务标识；URL保留任务上下文和必要筛选，刷新、返回、深链接及跨页跳转不丢失范围。使用统一业务对象标识与后端状态，避免每页各自维护互不一致的数据。

页面之间提供携带对象引用的操作，例如从资产及扫描结果进入漏洞收集、从漏洞关联进入利用链分析、从选中候选链进入验证、从验证结果进入报告。操作前检查前置产物；未满足时显示缺少的信息和补充入口。切换页面本身不启动、重跑或停止任务，后台执行持续进行。现有知识库、资源、模板、模型设置和助手能力继续保留，可通过主导航或辅助入口访问。

| 首版环节 | 主要任务 | 最小交付结果 |
|---|---|---|
| 资产发现/漏洞扫描 | 在授权范围内发现资产、识别服务并执行受控扫描；也支持导入已有结果 | 结构化资产清单、扫描发现、原始结果及证据引用 |
| 漏洞收集 | 围绕资产组件和扫描发现检索、整理漏洞信息，处理基本去重与适用性 | 漏洞记录、来源、影响范围、与资产的关联及待确认条件 |
| 利用链推理 | 结合资产关系与漏洞条件组织候选链，执行必要规则校验 | 可查看的路径、节点关联依据、证据、未知条件及验证建议；无链时明确说明原因 |
| 渗透测试 | 复用现有智能体和工具，在范围与执行约束内验证关键条件，整理反馈 | 验证步骤、执行状态、观察证据和分级结论；未执行或失败不得冒充验证成功 |
| 报告输出 | 汇总五阶段结果，关联资产、漏洞、候选链与验证证据 | 可查看并导出的中文报告，区分已确认、待确认、受阻断与失败，给出整改建议 |

首版保留以下必要工作：任务创建删除卡顿与状态反馈修复、前端全部静态文字中文化、最小资产/漏洞/候选链/证据对象、阶段间结构化交接、现有智能体复用、一个必要的利用链分析角色、执行范围检查、人工介入和现有功能回归。

首版不要求建设完整八模块平台、复杂多项目组织、独立情报运营中心、全量图谱基础设施、完整报告审批发布中心、整改工单平台、大规模分布式调度及完整科研对照实验。正式报告中的研究指标留待后续实验验证。领域关系与规则可先用现有数据库和任务范围数据实现，是否部署独立图服务取决于实际查询需求；不能用一段自由文本代替可校验的候选链记录。

推荐实际实施批次：

1. 稳定性与中文化：修复任务操作阻塞，完成全部现有前端静态文案中文化，保留原有功能入口。
2. 多页面与五阶段任务骨架：任务中心、四个研究环节页面与报告交付页面、跨页任务上下文、阶段状态、最小数据对象、输入输出契约和证据引用；各阶段只展示真实已有能力与产物。
3. 接通资产发现/漏洞扫描和漏洞收集，形成资产上下文中的漏洞记录。
4. 接通候选利用链推理及受控渗透测试，使验证结果回写对应条件和节点。
5. 完成中文报告与端到端走查，复核迁移、模型配置、文件、助手交互及报告导出未退化。

首版验收采用一个固定的授权隔离靶场任务：从创建任务开始，五个独立阶段页面均可操作，阶段结果可被下一阶段使用，最终报告中的主要结论可追溯到证据。检查跨页切换、刷新、返回及直接打开链接后任务范围一致、后台任务不被重复触发。另覆盖无发现、无候选链、接口失败、证据不足和用户停止等情况，确保流程可以明确结束或等待处理。完成标准是主要任务闭环可用，不以页面数量、智能体数量或候选链数量衡量。

## 1 改造结论与边界

建议保留 React/TypeScript 前端、Go 后端、模型接口、PostgreSQL/pgvector、任务消息与 Docker 工具运行能力，在其上新增业务模块和确定性控制层。工程形态先采用模块化后端加独立工具运行器，避免一开始拆分大量微服务。

报告中的五层分别是用户交互、多智能体协同、知识与状态管理、工具与执行、基础设施与模型支持，安全保障贯穿各层。主流程是扫描、收集、推理、利用，但知识收集是持续服务，不能实现成必须完成一次扫描才可访问知识库的单向流水线。

第一版面向报告第8章界定的隔离模拟环境，不将原型结论外推为真实铁路生产系统能力。铁路设备、协议和检测约束先通过结构化资料与样例接入，真实被动采集、专有协议识别及专用设备适配分别规划。第2、3节中的完整业务模块和数据对象作为能力清单，按阶段选择实现，不作为首版必须一次性交付的范围。

## 2 已有能力与缺口

| 报告要求 | 当前可复用能力 | 必要改动 |
|---|---|---|
| 测评项目与统一任务 | Flow、Task、Subtask、Assistant | 增加项目、授权范围、阶段、依赖图、审批与任务快照；保留 Flow 作为执行上下文 |
| 资产与服务组件管理 | 终端工具、文件上传、任务输出 | 新增资产实体、地址与接口、服务组件、观测来源、归并与变更历史 |
| 漏洞知识与资产漏洞实例 | 知识存储、搜索工具、向量检索 | 新增漏洞实体、影响版本、资产漏洞关联、候选与确认状态、情报同步 |
| 知识关系与利用链 | 可选 Graphiti/Neo4j 集成和关系检索 | 新增领域模式、规则校验、有界路径生成、条件状态、可信度和风险分项 |
| 多智能体协同 | 现有代理角色、提示词、工具调用和日志 | 增加报告中的业务角色及结构化输入输出、共享事实引用、阶段调度 |
| 执行安全 | Docker、工具超时、用户交互、调用限制 | 建立独立策略网关、能力白名单、任务级授权、审批、资源和网络限制 |
| 证据与结果报告 | 工具日志、截图、任务文件、Markdown/PDF 报告 | 新增证据对象、完整性校验、事实判定、审核发布、版本与复测 |
| 中文业务界面 | 路由、侧栏、列表和详情组件 | 全量静态文案中文化，新增任务中心、四个研究环节页面与报告交付页面，共享任务上下文及证据入口 |
| 实验评价 | 测试器、调用记录、观测接口 | 新增人工活动计时、真值集、实验快照、配对对照与有效发现去重 |

注意：向量相似度检索不能代替版本判断、网络可达性判断或利用链规则推理；启用 Graphiti 也不等于获得报告要求的利用链引擎。终端日志不能直接等同于已审核证据，聊天中的确认不能直接等同于绑定目标与参数的执行审批。

### 2.1 现有优势保留与完善清单

以下为已知优先保留项。P0 还需盘点源码和实际部署中的可用能力，记录当前行为与缺陷，补全清单；不能将仓库支持但未启用或未验证的功能标记为当前已正常运行。

| 现有能力 | 保留原则 | 改造方向与验收重点 |
|---|---|---|
| Automation 与 Assistant 双模式 | 保留自动执行、交互引导及任务中补充说明的入口 | 研究流程融入两种模式，支持查询状态和调整后续步骤；普通对话无需填写完整测评表单 |
| 专业代理协作与辅助规划 | 复用现有专业角色、规划、反思和执行监测能力 | 增加业务契约和证据要求；验证可选监督功能的额外用量与实际收益，按需启用 |
| 多模型与按角色配置 | 保留模型服务页面、角色级配置和自定义兼容接口 | deepseek-flash 是当前配置而非代码硬编码；校验保存、错误反馈与配置生效范围，记录任务配置快照 |
| 长上下文处理与知识记忆 | 保留摘要、历史知识检索和上下文管理 | 在摘要后保留证据标识与关键约束；正式事实和临时模型判断分层，避免检索串任务或串项目 |
| 多搜索来源与网页抓取 | 保留可配置的搜索和浏览器能力 | 纳入来源管理、超时与失败反馈，为漏洞情报增加结构化解析，不强制只保留单一来源 |
| 工具与容器管理 | 保留可扩展工具、运行隔离、超时和输出采集 | 接入能力注册与独立策略网关；保留工具用途，同时收紧可绕过授权控制的执行路径 |
| 任务文件与资源库 | 保留上传、资源附加、容器文件取回和下载 | 增加证据关联与来源元数据，迁移不丢失已有文件，明确文件快照与原始文件的区别 |
| 模板与提示词配置 | 保留现有模板复用和用户配置 | 提供中文任务模板与版本记录；更新默认模板不覆盖用户已保存内容 |
| 实时消息、工具轨迹与报告导出 | 保留消息流、终端输出、报告查看及 Markdown/PDF 导出 | 修复大日志导致的渲染压力和断线恢复；增加结构化证据报告，不删除已有便捷阅读与下载路径 |
| REST、GraphQL 与 API Token | 保留自动化集成入口 | 接口优先向后兼容；新增业务对象使用一致权限校验，变更需说明迁移方式 |
| 可选图谱与观测集成 | 保留 Graphiti、Langfuse、监控等集成能力 | 与领域图谱、实验记录对接；按需部署，避免强制全量服务增加本机负载 |
| 数据持久化与便捷部署 | 保留 Docker 启动、现有任务数据和配置 | 使用版本化镜像、数据库迁移和恢复检查；升级后现有任务、文件、模板和模型配置可访问 |

### 2.2 防止功能退化的交付规则

1. P0 建立“现有功能—当前状态—问题—对应测试—改造后结果”清单，既检查报告要求，也检查原有能力。不能把已有缺陷当成要保留的行为。
2. 优先扩展现有服务与页面。已有实现满足目标时直接复用；只有确定的功能、可靠性或权限边界缺口才触发替换，并记录取舍与迁移方式。
3. 每阶段检查受影响的原有流程：双模式创建与交互、停止与删除、模型配置、文件与资源、知识检索、模板、报告导出和相关 API。测试按变更范围选择，避免无意义重复全量测试。
4. 新业务流程先通过独立入口或功能开关落地，保留旧数据读取与回退路径。数据库迁移、文件关联和配置转换必须检查幂等性与数据完整性；正式迁移不直接用开发样例覆盖用户内容。
5. 将响应时间、失败恢复、内存与并发、模型用量、证据质量和中文易用性作为共同评价维度。修复任务卡顿和不明确的状态反馈，与研究能力建设同等重要。
6. 删除、替换或限制已有能力必须写明实际原因及等效使用路径。执行权限受控化仍需落实，不能为了保留通用终端的自由度而留下绕过任务授权的路径。

## 3 数据对象与状态先行

建议新增以下数据对象，命名在设计阶段冻结：

- 项目与约束：Project、Scope、Authorization、PolicyVersion。
- 环境事实：Asset、Interface、Service、Component、Observation、Reachability。
- 漏洞知识：Vulnerability、AffectedVersionRange、IntelligenceSource、KnowledgeRevision。
- 检测结果：ScanJob、Finding、AssetVulnerability，分别表达任务、原始发现和资产上下文中的漏洞实例。
- 利用链与验证：CandidateChain、ChainNode、ChainEdge、Condition、VerificationTask、TaskDependency。
- 执行与证据：Capability、ExecutionRequest、Approval、ExecutionRecord、Evidence、EvidenceLink、AuditEvent。
- 交付与实验：ReportVersion、Remediation、Retest、ExperimentRun、HumanActivity、GroundTruthReference。

所有对象使用稳定标识，并包含项目范围、来源、时间、版本和关联证据。避免用 IP 作为唯一资产身份，也避免用 CVE 编号作为资产漏洞实例的唯一身份。复用观测与判定规则时需要保留旧版结果和重新计算原因。

至少分开维护运行状态、判断状态、约束状态、数据状态和整改状态。例如“工具执行完成”不能自动将漏洞改成“已验证”，“未执行”也不能自动改成“无漏洞”。模型推测先进入候选层，经工具、规则或人工复核支持后才成为正式事实。

PostgreSQL 保存正式记录和状态；pgvector 保存文档检索索引；图存储保存可重建的领域关系投影；文件存储保存原始材料及哈希。采用事务事件记录或 outbox 同步图与向量索引，支持重放、失败重试和版本落后提示，不让一次请求同步写入多个存储后出现半完成状态。

### 3.1 首版存储职责与一致性契约

首版以 PostgreSQL 为唯一事实源，不要求先部署 Neo4j。图查询先用关系表/递归 CTE 实现；只有冻结样例表明查询性能或表达能力不足时，才启用 Graphiti/Neo4j 作为可重建投影。pgvector 只保存文档块向量及检索元数据，不保存正式判定。

| 存储 | 首版保存内容 | 一致性规则 |
|---|---|---|
| PostgreSQL | 项目、范围、资产事实、漏洞知识、条件、任务、状态、审批、证据元数据、报告快照、outbox | 所有正式状态转换在单库事务内完成；行带 `project_id`、`revision`、`created_at`、`updated_at` |
| 对象/文件存储 | 原始扫描输出、公告/资料快照、日志、截图、报告文件 | 写入后计算 SHA-256；数据库只引用不可变对象版本，不用解析结果覆盖原件 |
| pgvector | 审核通过文档的分块、向量、来源与知识版本 | 由 outbox 异步生成；检索结果必须回指原文块，索引落后时显示版本提示 |
| 图投影（可选） | 资产、组件、漏洞、条件、权限、可达性及证据关系 | 只消费 outbox；可按 PostgreSQL 快照重建；投影失败不回滚正式事实，也不能发布依赖缺失投影的新结论 |

每个写接口接收 `idempotency_key`；数据库以 `(project_id, operation_type, idempotency_key)` 去重。事务提交时同时写业务记录和 outbox。消费者按 `event_id` 幂等处理，指数退避并进入死信状态；管理页显示最新成功投影版本、积压量和最后错误。删除业务对象默认采用软删除/失效版本，已被证据或报告引用的版本禁止物理删除。

### 3.2 最小数据表与关键字段

设计评审时可调整物理表名，但以下语义不能合并丢失：

| 表/聚合 | 首版关键字段 |
|---|---|
| `research_projects` | `id`、`name`、`status`、`owner_id`、`current_policy_version_id` |
| `project_scopes` | `project_id`、目标类型、规范化目标、允许安全域、业务窗口、允许风险上限、有效期 |
| `authorizations` / `policy_versions` | 授权文件引用、审批人、有效期、规则 JSON、版本哈希、状态 |
| `assets` / `asset_identifiers` | 稳定资产 ID、类型、重要性、安全域；IP/MAC/主机名/序列号/厂商设备 ID 及来源 |
| `observations` | 观察类型、规范化值、原始值、来源、采集时间、有效期、置信度、证据 ID、解析器版本 |
| `services` / `components` | 资产 ID、协议/端口、产品标识、版本原文、规范化版本、补丁状态、状态及证据 |
| `vulnerabilities` / `affected_ranges` | CVE/CNVD/厂商编号、CWE、严重度；生态、CPE/PURL、范围表达式、修复版本、解析状态、来源修订 |
| `findings` / `asset_vulnerabilities` | 扫描批次、规则 ID/版本、资产、漏洞、原始结果；判断状态、适用条件快照、判定规则版本 |
| `conditions` / `state_facts` | 条件类别、主体/客体、运算符、期望值、当前状态、有效期、支持/反对证据 |
| `candidate_chains` / `chain_nodes` / `chain_edges` | 分析快照、节点顺序、前置条件、预期效果、衔接状态、剪枝原因、可信度分项、风险分项 |
| `verification_tasks` / `task_dependencies` | 验证目标、输入事实版本、预期观察、证据门槛、风险、环境、依赖类型、运行状态 |
| `capabilities` / `execution_requests` | 能力及适配器版本、参数 schema、证据 schema、风险基线；目标、参数摘要、策略版本、审批摘要 |
| `execution_records` / `evidence` | 调用状态、退出原因、前中后基线、资源使用；文件哈希、采集主体、时间、解析状态、审核状态 |
| `report_versions` / `experiment_runs` | 数据快照版本、模板/模型版本、审核状态；场景、分组、真值版本、时间预算、人工活动与结果 |

### 3.3 可复算状态机

资产—漏洞判断只允许以下状态：`candidate`（待确认）、`confirmed`（已确认）、`not_detected`（未检出）、`conflict`（冲突）、`stale`（过期）。`not_detected` 只有在目标、能力版本、必要凭据、业务窗口和检测步骤全部满足且完整执行时才能产生，并保存“覆盖了哪些条件”的检测快照；超期或资产关键属性变化后转为 `stale`，不能继续展示为无漏洞。

条件状态固定为 `satisfied`、`partially_satisfied`、`unsatisfied`、`unknown`、`conflict`。验证结论固定为报告表 6.4 的七类：已验证成立、条件成立但未执行高风险动作、条件不成立、证据不足、受约束阻断、执行失败、需人工复核。运行成功只更新执行事实，不直接更新漏洞结论。

状态转换由后端规则服务执行，并记录 `from_state`、`to_state`、`rule_version`、`input_revision`、`evidence_ids` 和原因。来源冲突时同时保留支持与反对证据，生成最小补证任务；模型只能建议状态及理由，不直接提交正式状态。

### 3.4 资产归并与版本匹配的落地算法

资产归并采用“确定性键优先、候选评分、人工合并”三步：

1. 同项目内以设备序列号/云资源 ID/经过验证的 MAC 等强标识精确归并；IP 只作为可变地址，不作唯一键。
2. 对没有强标识的记录，根据主机名、MAC、厂商型号、证书指纹、服务集合、网络位置和时间重叠计算候选分。硬冲突（两个有效序列号、同一时刻不可能共存的位置）直接禁止自动合并。
3. 只有达到冻结阈值且没有硬冲突时自动合并；中间分数进入人工复核，低分新建资产。每次合并保留成员观测、算法版本、分项得分，并支持拆分回滚。

版本处理不使用字符串大小比较。采集时同时保存 `version_raw`、`version_normalized`、`ecosystem` 和 `parse_status`。首版实现 SemVer、Debian (`dpkg --compare-versions`) 与 RPM (`rpmvercmp`) 三种比较器；CPE/PURL负责标识产品与生态，不能代替版本比较。无法确定生态、厂商自定义版本、通配/排除范围或缺少 epoch/revision 时返回 `unknown` 并生成补证，不猜测适用。所有比较器使用 NVD/厂商公告提取的冻结夹具做边界、开闭区间、预发行版和异常输入测试。

### 3.5 工具适配器与首批接入范围

适配器统一实现 `Describe`、`Validate`、`Plan`、`Execute`、`Cancel`、`Parse`、`Health`。`Describe` 返回适用资产/协议、参数 JSON Schema、证据类型、交互强度、风险基线、凭据要求和工具版本；`Plan` 只生成规范化请求；`Execute` 只能由执行网关调用；`Parse` 同时返回结构化结果和原始输出引用。每次调用必须支持超时、取消、资源上限、输出大小上限和关联标识。

首批范围收紧为：

- 输入：Nmap XML 与一类现有 PentAGI 工具 JSON/日志；若第二类尚无稳定 schema，则改用人工导入模板，不承诺任意文本全自动解析。
- 主动能力：Nmap 的白名单低频服务识别，以及一个针对隔离靶场的只读 HTTP/TLS 核验能力。首版不自动运行通用 exploit 框架。
- 情报：NVD API/JSON、CISA KEV、MITRE CWE/ATT&CK 官方数据、厂商公告人工上传；CNVD 仅在许可、频率和接口条件确认后接入。
- 铁路资料：仅导入已获授权并完成脱敏/许可登记的设备、协议和运维约束样例。RSSP-I/RSSP-II 首版只做文档知识与被动特征记录，不实现生产报文主动探测。

### 3.6 有界图搜索与可解释评分

首版路径生成采用确定性、可复算的束搜索：从已确认初始状态出发，按前置条件匹配扩展节点；默认最大深度 5、每节点最大分支 20、每条路径最多 2 个 `unknown`、单次分析 30 秒、最多保留 100 条路径。以上均为安全默认值，需通过冻结样例和资源测试后才能调大。`unsatisfied`、越权目标、安全域方向冲突立即剪枝；`conflict` 不进入高可信路径；无新增状态的重复节点按路径签名去环。

可信度与风险分开保存，不使用一个模型分数混合表达。可信度分项至少包含产品版本、条件覆盖、可达性、状态衔接、证据质量、时效性和未知/冲突惩罚；风险分项至少包含入口暴露、漏洞严重度、预期权限、安全域跨越、资产/业务重要性和恢复难度。权重与阈值存为版本化配置，输出分项、命中规则、剪枝点和证据 ID；大模型仅把已检索文本映射为候选条件/效果与解释，程序校验 schema 和证据引用后才参与路径计算。

### 3.7 风险、审批、停止与恢复矩阵

| 等级 | 自动化边界 | 审批 | 运行控制 |
|---|---|---|---|
| R0 | 脱敏后的离线分析 | 项目授权有效即可 | 禁止目标网络访问，记录模型/提示词/资料版本 |
| R1 | 白名单内被动采集、只读查询 | 策略预授权，可按任务抽查 | 目标/网段/安全域三级限频，凭据最小化，全程留痕 |
| R2 | 仅授权测试环境的低扰动、可逆验证 | 单次请求审批，绑定目标、参数哈希、有效期 | 执行前后基线、配额、并发限制、自动熔断和恢复检查 |
| R3 | 仅隔离仿真环境的状态改变操作 | 两名不同审批者；快照和回退演练通过 | 独立运行器、网络隔离、短时凭据、人工监护、强制检查点 |
| R4 | 不实际执行 | 不提供放行路径 | 仅离线推演或安全等效替代 |

任一请求在执行前重新校验项目授权、规范化目标、策略版本、参数哈希、业务窗口、能力版本和环境证明。参数、目标、能力或策略变化使审批失效。停止条件至少包括目标服务异常、响应时延/错误率超过基线阈值、资源超限、证据写入失败、业务窗口结束、人工紧停和连续失败达到上限；停止后取消同一依赖分支，记录未执行原因，R2/R3 再执行恢复检查。阈值由项目策略配置，未配置时主动能力不得执行。

### 3.8 五阶段输入输出与失败语义

| 页面阶段 | 必要输入 | 确定性处理 | 可交付输出 | 不成功时的明确状态 |
|---|---|---|---|---|
| 资产发现/扫描 | 项目范围、授权、能力版本、导入文件或执行请求 | 解析、归并、范围校验、候选漏洞匹配、检测覆盖检查 | 版本化资产画像、Finding、AssetVulnerability、原始证据 | 越权/受阻断、解析失败、部分完成、证据不足、未检出条件不完整 |
| 漏洞收集 | 资产组件、漏洞线索、来源白名单、知识截止时间 | 增量采集、实体归一、版本解析、来源冲突识别 | 知识修订、影响范围、适用/检测条件、来源证据 | 来源不可用、许可受限、解析未知、冲突待复核、索引落后 |
| 利用链推理 | 冻结事实快照、漏洞状态、网络可达性、条件/效果规则 | 子图提取、规则校验、有界搜索、去环/剪枝、分项评分 | 候选链、连接依据、未知项、补证清单、分析终止原因 | 无合法路径、超时截断、关键条件未知/冲突、数据过期 |
| 渗透测试 | 经复核候选链、验证任务图、授权/审批、环境证明 | 依赖调度、网关复核、受控执行、基线监测、证据判定 | 四类事实、执行记录、七类验证结论、知识反馈事件 | 等待审批、受约束阻断、执行失败、证据不足、已停止/恢复失败 |
| 报告输出 | 已冻结数据快照、审核状态、模板版本 | 确定性装配字段和引用，模型只撰写有来源叙述 | 中文报告、证据附录、整改建议、复测比较 | 缺证据禁止发布、数据过期需重算、审核退回、导出失败 |

每阶段 API 都返回 `status`、`reason_code`、`input_revision`、`output_revision`、`evidence_ids` 和可执行的下一步；页面不得用虚假百分比或模型自由文本代替这些状态。

## 4 多智能体如何调整

报告定义的是业务职责，不要求十个独立模型服务长期同时运行。现有 13 个模型角色属于底层能力分工，与报告角色不是一一对应关系。第一版只计划新增专门的利用链分析智能体，其他业务职责优先通过已有角色的提示词、工具权限和结构化数据接口实现。

| 报告业务角色 | 建议实现 |
|---|---|
| 主协调智能体 | 在现有主代理上增加阶段状态与任务路由；只组织流程，不直接写入未经验证的事实 |
| 资产发现与漏洞扫描 | 复用 pentester、installer，接入资产服务、观测解析、检测约束和受控扫描能力 |
| 漏洞情报与知识检索 | 复用 searcher、enricher，补充固定情报源、结构化提取、归一、来源冲突处理和授权范围内检索 |
| 利用链分析 | 新增专门角色，调用规则与图查询引擎，组织候选路径、解释关联并列出待补证条件 |
| 利用决策与验证执行 | 复用 primary_agent、pentester、coder，维护验证任务依赖并生成经网关检查的能力请求 |
| 评估反馈 | 复用 reflector、adviser，检查证据充分性，区分失败、未知、否定并提出调整建议 |
| 证据与报告 | 复用现有报告生成流程，引用正式记录，交由确定性校验和人工审核 |

如果后续测试发现职责冲突、上下文混乱或质量不稳定，再考虑拆出漏洞情报或证据评估智能体。程序承担版本匹配、条件判断、权限检查和状态转换，不能仅靠增加智能体数量解决这些问题。

初期继续使用已配置的 deepseek-flash 和百炼向量服务。按角色保存模型、提示词和能力集版本，后续通过实验判断是否需要更换部分角色的模型，而不是预设全 Flash 能达到报告的研究指标。

## 5 分阶段实施与验收

### P0 固定开发版本并解决任务阻塞

状态：**基本完成**。创建、停止、结束和删除已使用持久作业、关联标识、分段耗时、有限重试和重启恢复；生命周期同步已按流程隔离，后台执行器最多并发处理 4 个不同流程且保持同一流程 FIFO。慢初始化不再阻塞另一流程停止，相关竞态测试已通过。

关闭 P0 还需：把现有回归数据和迁移版本作为发布基线归档；持续监控作业积压、失败率和 p95；后续新增生命周期作业必须复用同一按流程串行契约。当前单套部署不再执行旧计划中的独立开发环境与备份要求。

验收现状：慢初始化跨流程停止、不同流程并发、同流程串行、失败重试、进程恢复和重复提交已覆盖；真实栈受理 p95 已远低于 2 秒目标。P0 不再阻塞 P1。

### P0A 前端全部静态文字中文化

状态：**基本完成**。全站静态中文化、错误码中文映射、主要路由/断点页面走查、PDF 表格与跨页表头、axe/辅助技术树/纯键盘检查已经完成；本阶段只剩 NVDA/讲述人真实语音人工走查和后续新增页面的持续门禁。

范围包含登录与账号、仪表盘、侧栏导航、Flow 新建及详情、Assistant、任务与子任务、知识库、资源与文件、模板、报告、模型提供商、提示词和其他设置中的所有用户可见静态文字。覆盖菜单、按钮、标题、标签、表头、筛选与排序、分页、输入占位符、帮助说明、悬浮提示、弹窗、确认与取消、通知、校验错误、空状态、加载状态、异常页面、状态枚举显示、图表图例、复制与导出反馈。

同时检查浏览器页面标题、HTML lang、日期与相对时间、第三方组件默认文案、无障碍标签和屏幕阅读器提示、前端生成的报告/PDF中的固定标题与表头。登录失败、权限不足、请求超时等面向用户的错误应通过稳定错误码映射为中文说明，原始诊断内容放在可展开详情中，不直接用英文错误替代中文提示。

实施步骤：

1. 盘点路由、组件和公共文案，建立页面与交互状态清单。
2. 建立统一中文术语和集中维护的文案机制；Flow 显示为“任务流程”，Task/Subtask 显示为“任务/子任务”，Provider 显示为“模型服务”，保持层级与语义一致。
3. 逐页替换硬编码文案并配置第三方组件中文区域设置；新增页面使用同一文案机制。本期不要求增加语言切换功能。
4. 区分界面文案与机器标识：模型名、工具名、产品专名、接口字段、路由、配置键、命令及协议名保留正确技术标识，转换其周边说明和显示标签。
5. 用户输入、外部原文、终端日志和模型动态回答不属于静态文字翻译；不改写用户保存的提示词或破坏代码示例。模型中文输出作为单独的提示词与报告配置事项。
6. 构建自有镜像并验证实际运行页面，确保源码中文化已进入部署。

验收：覆盖全部现有路由及主要成功、失败、加载、空状态和弹窗；除必要技术标识与原始内容外，不遗留用户可见的英文静态文案。结合文案扫描、页面走查和代表性端到端交互检查；调整中文长度造成的截断、换行、按钮宽度和 PDF 字体问题，确认图标按钮的无障碍名称也为中文。不得以批量替换所有英文字符串的方式修改接口契约或生成类型。

### P1 项目范围与证据基础

目标：先形成后续阶段共同使用的事实源，不在本阶段接入主动工具。

实施顺序：

1. 冻结第 3.2 节最小字段、枚举和迁移回滚脚本；先落 `Project/Scope/Authorization`、资产与观察、Evidence、阶段运行、outbox。
2. 提供资产台账 CSV/JSON 与 Nmap XML 导入；导入采用“校验预览 → 确认提交”两步，错误行可下载，重复提交由幂等键去重。
3. 实现第 3.4 节归并服务及人工合并/拆分，任何属性均能回到 Observation 和原始文件。
4. 建立任务中心和五阶段路由骨架，共享 `project_id/task_id`；证据侧栏显示原件哈希、解析版本和审核状态。
5. 所有查询在服务层强制项目范围过滤，直接按对象 ID 查询也必须复核项目归属；增加跨项目负向测试。

出口条件：冻结样例中同一资产多来源归并正确且可拆分；重复导入不增加记录；冲突字段同时可见；缺少授权不能创建主动阶段；随机抽取的正式事实均能打开原始证据。数据库迁移可向前、向后各演练一次。

### P2 受控执行与持久任务调度

在接入主动扫描和验证前建立最小工具网关。请求必须绑定项目、目标、能力版本、参数摘要、风险、有效期和证据要求。每次实际执行前重新检查权限和策略，不让模型自报风险等级决定放行。

按报告实现 R0 至 R4：R0 离线分析；R1 经授权被动或只读核验；R2 授权测试环境的低扰动可逆验证；R3 仅隔离仿真且具备双重审批和恢复条件；R4 不进入实际执行。审批绑定请求内容及有效期，参数或策略变更后失效。

将业务验证能力封装为第 3.5 节接口。首批只实现模拟适配器、Nmap 低频识别和只读 HTTP/TLS 核验；原任意 Shell、动态安装和直接 Docker 通路继续服务旧 Flow 时必须标记为 legacy，并禁止从新研究任务入口调用。工具运行器独立承载资源配额、网络访问限制、停止信号和证据写入；主控服务只发签名后的结构化请求。

调度以持久 `verification_tasks` 为源：依赖满足后才进入 ready；领取使用租约和幂等执行键；心跳失效后可恢复为 queued，但产生过目标状态变化的任务必须人工复核后再运行。R2/R3 的执行前基线和恢复步骤也是依赖节点，不是提示文本。

出口条件：策略测试矩阵覆盖越界目标、DNS/IP 解析变化、过期或被篡改审批、参数/能力/策略变更、窗口结束、禁用能力和错误环境；停止在时限内传递到运行器并阻断依赖步骤；证据落库失败时执行不能成为成功结论；legacy 通路无法绕过新任务入口网关。

### P3 扫描与多源漏洞知识

先按第 3.5 节范围接入 Nmap XML 和一类稳定结构化结果；完成统一解析、证据入库、资产归并、候选漏洞生成和检测任务依赖图。扫描任务分为可复用的共享检测与漏洞专用检测，能力登记必须包含适用对象、协议、证据类型、交互强度、风险、凭据、工具版本和历史效果。

情报接入先采用官方允许的 API、公开数据文件和人工上传，保存原始快照、来源、更新时间与解析版本，再扩展增量同步、去重、失败处理和字段冲突。模型用于非结构化提取与解释，版本匹配等规则由程序执行。CNVD 等来源的访问条件需单独确认，不承诺无限制实时采集。

铁路资料第一版只导入已核准的设备、组件、协议和检测约束样例。每份资料记录权利/许可、密级、脱敏策略、可调用模型范围和有效时间；未登记的数据禁止发往云模型。机器学习资产分类、历史成功率预测只有在形成经审核训练集、标签定义和基线后进入实验，不作为 P3 首版承诺。

候选匹配顺序为产品/组件标识 → 生态比较器 → 补丁与配置条件 → 服务/协议条件。无法解析的版本、冲突来源或过期观察保持 `candidate/unknown/conflict`。`not_detected` 必须满足第 3.3 节完整检测条件。

出口条件：冻结样例覆盖别名、CPE/PURL、SemVer/Debian/RPM 边界和未知生态；原始结果到资产漏洞实例可追溯；重复告警不重复计数；来源修订或资产变化产生 outbox 并把受影响实例标为待复核/过期；公开知识与项目私有资料权限隔离；采集失败可断点续传且不覆盖旧修订。

### P4 候选利用链与验证任务图

定义领域图模式，明确资产、服务、组件、漏洞、条件、权限状态、安全域及证据之间的关系。用规则判断结构化条件，以检索增强的模型补充自然语言条件解释。

按第 3.6 节默认边界实现路径生成、重复路径归并、约束剪枝和变化后的局部重算。业务依赖与技术可达性使用不同关系类型；跨资产边必须同时有方向、协议/服务和有效观察，缺失时只能保留为低可信候选。每条边保存条件衔接依据和证据引用；条件使用满足、部分满足、不满足、未知、冲突等状态。分别保存可信度与风险分项，未知条件不能当作成立。

把候选链转为具有输入条件、依赖、预期观察、证据门槛及执行限制的验证任务图。界面提供关系图、路径列表、节点条件、证据和复核清单；图展示由后端结构化记录驱动。

出口条件：用至少包含单资产链、跨资产链、环、版本不符、不可达、未知、冲突和越权目标的冻结合成图做 golden test；相同输入与规则版本产生相同路径签名；超限时返回截断原因而非静默漏链；新增/失效证据只重算受影响子图；关闭模型后确定性路径仍可生成，模型不可把缺失事实补成已确认；候选链与实际验证结果分别展示。

### P5 受控验证反馈与报告

由现有智能体承担验证阶段的决策、检索、执行、评估和报告职责，接通任务图，不新增五套独立智能体。形成条件事实、执行事实、效果事实和约束事实四类记录；将结果映射为已验证、条件成立但未执行、条件不成立、证据不足、受约束阻断、执行失败、需人工复核等结论。

验证过程中在执行前、中、后采集基线、运行记录、目标侧直接观察和恢复结果。评估服务按规则输出报告表 6.4 的七类结论；模型生成的解释必须列出 evidence ID，无法解析或引用不存在的证据时只保存为草稿建议。

报告从正式数据快照装配，模型负责有引用的叙述。增加草稿、审核中、已发布、复测版及模板版本，支持证据附录、整改事项和复测比较。技术漏洞判断与等保要求项评价分别保存。发布时冻结 SQL 数据修订、文件哈希、规则/提示词/模型/模板版本；投影落后、关键证据缺失或存在未处置冲突时阻止发布。

出口条件：工具退出成功不自动认定漏洞成立；对七类结论逐类进行契约测试；高风险结论缺少直接观察或独立复核时阻止发布；从已发布快照可在断开模型服务的情况下重建相同事实表和证据目录；复测按资产漏洞稳定 ID 区分新增、未变化、关闭与再次出现。

### P6 实验与阶段验收

按报告第8章采用传统非AI完整流程组与完整AI系统组配对比较，冻结相同拓扑、工具、知识截止时间、时间预算、真值和证据门槛。预实验先验证环境恢复与记录流程，其结果不进入正式验收统计。

人工介入时间包含审批、复核、异常接管与报告审核，不包含机器自主运行和排队等待；覆盖率按资产上下文中的唯一有效漏洞实例统计。报告提出的人工时间降低不低于15%、有效漏洞覆盖率相对提升不低于15%是待实验验证的目标，不是功能完成即可保证的结果。

正式实验采用场景内配对，并对等价场景顺序做平衡或随机化以降低学习效应。预注册每组样本数、重复轮次、排除规则和统计方法；样本数在预实验方差结果基础上做功效分析，不预先凭经验写死。对配对人工时间和覆盖率差报告均值/中位数、95% 置信区间和配对效应；当传统组覆盖率为 0 时，相对提升率无定义，必须同时报告百分点差并按预注册规则处理，不能用除零后的任意数通过验收。

人工活动通过显式开始/结束事件记录，审批、复核、异常接管和报告审核计入；机器运行、排队不计入但另报端到端时间。真值集由未参与当轮操作的人员冻结，正式运行时对两组隐藏；同一 CVE 在不同资产按不同实例计数，同一实例的多工具发现只计一次。

出口条件：环境快照可重复恢复；两组使用相同拓扑、工具版本、外部数据截止、时间预算、终点和证据门槛；验收同时报告精确率、召回/覆盖率、百分点差、总耗时、资源、证据完整性和安全事件。出现越界、测试致服务中断、关键证据缺失或两组终点不一致的轮次按预注册规则剔除并披露。第一版不开展真实铁路生产网络实验，15% 两项是待验证研究目标而非交付保证。

## 6 代码改动落点

以下现有路径已检查；“新增”路径为建议，并非当前已有功能。数据库生成文件、GraphQL 类型应通过生成流程更新，不直接手工编辑。

| 范围 | 现有修改入口 | 建议新增模块 |
|---|---|---|
| 全量中文化与多页面 | frontend/index.html；frontend/src 下的 pages、features、components、providers、lib；重点包括路由、侧栏、Flow 表单及报告组件 | 统一中文文案、错误映射与区域设置；任务中心及扫描、收集、利用链、渗透测试、报告页面；共享任务上下文与证据组件 |
| 创建删除与调度 | backend/pkg/controller/flows.go；backend/pkg/controller/flow.go；backend/pkg/server/services/flows.go；frontend/src/providers/flows-provider.tsx | jobs、workflow、task dependency 和持久作业记录 |
| 数据与 API | backend/sqlc/models；backend/pkg/server/models；backend/pkg/server/services；backend/pkg/graph/schema.graphqls | 项目、事实、资产漏洞实例、证据、审批、报告数据模型及迁移 |
| 代理协同 | backend/pkg/providers；backend/pkg/templates/prompts | 业务角色配置、结构化契约、阶段路由与上下文构建 |
| 知识与关联 | backend/pkg/graphiti/client.go；backend/pkg/tools/graphiti_search.go；backend/pkg/providers/embeddings | intelligence、facts、knowledge schema、chains、投影同步 |
| 工具执行 | backend/pkg/tools/tools.go；backend/pkg/tools/terminal.go；backend/pkg/docker | policy、capabilities、execution gateway、runner |
| 文件与报告 | backend/pkg/flowfiles/files.go；frontend/src/lib/report；backend/pkg/templates/prompts/reporter.tmpl | evidence、audit、report snapshot、review、retest |
| 部署 | Dockerfile；docker-compose.yml；docker-compose-graphiti.yml | 开发镜像覆盖配置、独立运行器配置、资源与网络约束 |

## 7 第一版范围与推荐交付顺序

P0/P0A 已基本完成。后续不按“先做满一个大模块再开始下一个模块”，而按以下可演示纵向切片交付：

1. **切片 A（P1）**：项目范围 → 资产/Nmap 导入 → 归并 → 原始证据查看。冻结公共 ID、状态和 outbox 契约。
2. **切片 B（P2+P3 最小集）**：策略预检 → Nmap/HTTP 只读能力 → 解析 → 候选漏洞 → `confirmed/candidate/not_detected/conflict/stale` 状态。
3. **切片 C（P4）**：固定事实快照 → 确定性有界搜索 → 路径与未知条件 → 验证任务图；先在合成图上验收。
4. **切片 D（P5）**：审批 → 隔离靶场验证 → 前中后证据 → 七类结论 → 中文报告快照与复测。
5. **切片 E（P6）**：预实验 → 冻结统计方案 → 正式配对实验；实验不过不倒推篡改证据口径，而是记录未达到研究目标。

每阶段交付数据库与 API 契约、可操作页面、状态与异常处理、针对性测试、现有能力回归结果和回退说明。使用真实后端数据演示，不以静态页面冒充业务能力。前端在各阶段同步建设，不等全部后端做完才开始，也不先一次性画满八个空模块。

首版暂缓完整八模块管理平台、额外情报/证据专职智能体、真实铁路专有协议深度探测、生产高风险验证、大规模分布式采集、复杂机器学习资产分类训练与全量监控集群。对报告中的研究性能力逐项记录为已实现、部分实现或待验证。前端全部静态文字中文化不在暂缓范围内。

本机约16GB内存，适合低并发开发和小型演示。图数据库、知识服务、工具运行器和靶场叠加后需单独测资源，建议靶场或图服务按需启动，规模实验使用独立环境。五层逻辑架构不必一比一拆成五组常驻微服务。

## 8 开工前需冻结的设计项

默认采用现有仓库渐进改造、中文优先、单套开发部署、隔离模拟环境和当前云模型组合。以下为不可跳过的决策门：

| 决策门 | 必须冻结的内容 | 未通过时允许做什么 |
|---|---|---|
| G1：P1 迁移前 | 公共 ID、项目范围表达、资产归并强键/硬冲突、状态枚举、证据不可变规则、首个端到端样例 | 只做原型与迁移草案，不写正式业务数据 |
| G2：P2 主动能力前 | R0-R4 矩阵、审批绑定字段、默认限频/并发/停止条件、运行器网络边界、legacy 通路隔离方式 | 只允许导入和 R0 离线处理 |
| G3：P3 情报同步前 | 首批来源许可与频率、版本比较生态、铁路资料许可/密级/脱敏、知识修订策略 | 只使用冻结本地样例 |
| G4：P4 推理前 | 图模式、条件/效果词表、搜索边界、评分分项与 golden graph | 只展示单漏洞条件，不生成链 |
| G5：P5 验证前 | 靶场快照、能力清单、证据门槛、恢复演练、报告发布阻断规则 | 只生成验证计划，不执行 |
| G6：正式实验前 | 隐藏真值、样本与轮次、人员分配、时间记录、统计与排除规则 | 只做预实验，不报告 15% 验收结论 |

完整工期只能在 G1 评审和首个迁移样例通过后估算；工具和数据源每增加一种，都需单独估算适配器、许可、测试夹具和维护成本。

## 9 当前基线与完成度（2026-09-15）

| 阶段 | 状态 | 当前结论 |
|---|---|---|
| P0 生命周期与调度 | 基本完成 | 持久作业、失败重试/恢复、按流程锁、跨流程最多 4 并发、单流程 FIFO 已实现并通过竞态测试 |
| P0A 中文与基础可用性 | 基本完成 | 全站静态中文化、浏览器辅助技术树/键盘、报告 PDF 表格及跨页表头已核验；NVDA/讲述人真实语音走查待人工完成 |
| P1 项目范围与证据基础 | 未开始 | 下一阶段，应先冻结 G1 并交付切片 A |
| P2 受控执行 | 未开始 | 不应在 P1 前开放研究任务的主动能力 |
| P3 扫描与漏洞知识 | 未开始 | 先做收紧后的两个输入和两个低风险能力，不承诺全工具/全来源 |
| P4 利用链 | 未开始 | 先实现 PostgreSQL 事实源和确定性有界搜索，图数据库不是前置条件 |
| P5 验证与正式报告 | 未开始 | 现有通用 Flow/助手报告可复用展示和导出，但不等于证据驱动的研究报告闭环 |
| P6 实验 | 未开始 | 15% 指标尚无实验数据，不得表述为已实现 |

当前本地分支为 `codex/research-development`，部署入口为 https://localhost:8443。详细测试与部署证据以 `IMPLEMENTATION_STATUS.md` 为准。本计划从本节起不再保留已取消的双环境历史作为待办。

## 10 报告要求到工程交付的核对结论

| 报告关键要求 | 本计划落点 | 判断 |
|---|---|---|
| 四环节闭环与证据反馈 | 第 3.8 节、P1-P5 | 可实现；页面五阶段已明确为四环节加交付，不再混淆 |
| 低扰动资产感知与实体归并 | 第 3.4、3.5、3.7 节，P1/P3 | 可实现；首版以导入和低频只读能力为边界 |
| 多源漏洞知识与铁路增强 | 第 3.1、3.5 节，P3 | 有条件可实现；数据许可、脱敏和版本语义是前置条件 |
| 可解释利用链 | 第 3.3、3.6 节，P4 | 可实现；确定性规则和有界搜索为主体，LLM 不负责事实判定 |
| 多智能体协同 | 第 4 节，P5 | 可实现；落实为职责和能力隔离，不要求五个常驻模型服务 |
| R0-R4 受控执行 | 第 3.7 节，P2/P5 | 可实现；必须先隔离 legacy 执行通路并完成恢复演练 |
| 完整证据与报告 | 第 3.1-3.3、3.8 节，P5 | 可实现；原始证据不可变、结论可复算、发布可阻断 |
| 人工时间与覆盖率提升 ≥15% | P6 | 技术上可测但不能预先保证；需预实验、配对设计和置信区间 |
| 复杂机器学习/历史预测 | P3 暂缓项 | 当前缺少合格训练数据，不纳入首版，待数据基线成立后再实验 |
