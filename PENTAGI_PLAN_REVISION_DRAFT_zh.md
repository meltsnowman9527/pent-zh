# PentAGI 研究平台方案修订讨论稿（v3 草案）

> 历史讨论留档：保留原文观点与决策过程。后续实施以 [开发方案](docs/research-platform/PLAN.md) 和 [开发契约](docs/research-platform/CONTRACTS.md) 为准；本文中的建议、待确认事项及完成声明不代表当前代码状态。

日期：2026-09-15 · 基线：分支 `codex/research-development`，HEAD `f4e300a`
用途：根据你 2026-09-15 两轮确认（D1–D13），把实施方案重排为"一次做对、精度优先"的版本；本文是讨论稿，待剩余决定点确认后转为正式方案。

---

## 0. 本轮已确认的决策（记录存档）

| 编号 | 决策 | 影响 |
|---|---|---|
| D1 | 实验方案、15% 指标、真值集、统计检验**暂不考虑** | 不再作为设计前置；建议保留最小埋点（见 §1.9） |
| D2 | 软著**需要**，论文暂不需要 | 需要有可演示、可命名、可截图的功能面；机器学习不宣称即可 |
| D3 | 机器学习内容**弱化**，后续按需再定 | 资产分类先用规则+统计+置信度，保留模型版本字段 |
| D4 | 靶场在**独立环境**，暂不处理 | 本机不跑 Vulhub/Juice Shop 等；夹具先用导入数据 |
| D5 | 阶段主流程：漏洞收集 → 资产发现与定向扫描 → 利用链推理 → 多 Agent 渗透验证 → 报告输出 | 界面顺序以此为准（见 §1.1 的依赖修正） |
| D6 | 两种使用模式：分页面手动操作 + 一键执行（类 PentAGI Flow） | 决定阶段状态机的设计（见 §2.2） |
| D7 | 全流程由多智能体完成；用户可自行配置 Skill 与 MCP | 需要能力注册 + MCP 客户端 + 安全边界（见 §1.2、§6） |
| D8 | 优先"把整体流程跑通"，细节后置 | 实施顺序改为端到端优先（见 §6） |
| D9 | 一键模式**仿照 PentAGI 双链路**：自动执行模式 + 交互助手模式 | 复用现有 Automation/Assistant 两条链路；两种模式共用同一阶段状态机（见 §2.4） |
| D10 | 审批按需加入 | 审批记录与队列从第一版就建：R0/R1 预授权直接执行，R2/R3 强制审批（R3 需两名不同审批者） |
| D11 | **精度与准确度优先** | 判定规则与证据链一次做对；精度靠夹具与黄金测试保证，不靠指标统计（见 §1.10） |
| D12 | 图谱要可视化，**能上 Neo4j 就直接上** | 采用报告的三层存储（结构化事实 / 图谱关系 / 原始证据）；Neo4j 作为领域图谱的**确定性投影**（见 §1.6） |
| D13 | 能直接做好的就做到最优 | 区分"不可逆项"与"可逆项"：不可逆项一次到位，可逆项持续加深（见 §1.11） |

---

## 1. 我认为需要修正的地方（逐条讨论）

### 1.1 阶段顺序可以按你的排法，但**运行期依赖方向必须反过来**

你的表述是：漏洞收集（含"系统内部资产信息比对得到疑似资产-漏洞集合"）→ 资产发现与扫描。

问题：**首次运行一个任务时还没有资产**，"内部资产信息比对"没有输入。这不是文字问题，而是流程依赖问题——如果按字面实现，第一阶段的输出会依赖后面阶段的产物。

建议（保留你的页面顺序，只修正依赖）：

- **漏洞收集 = 常驻知识服务**，而不是流水线的第一道闸门。报告本身也这么写：知识收集是持续服务，不能实现成"必须完成一次收集才能访问知识库"的单向流水线（方案 §1 第 64 行对应报告 294）。
- 采集与图谱构建：外部源 + 用户上传 → 结构化事实/关系/文档索引，**后台定时跑**，同时也允许在页面上手动触发。
- 页面顺序仍按你说的：**漏洞收集与知识图谱 → 资产发现与漏洞扫描 → 利用链推理 → 渗透测试验证 → 报告输出**。但"按当前资产重新匹配"这个动作放在**资产发现有结果之后**，入口可以同时出现在漏洞收集页和扫描页（"用当前资产重新生成疑似漏洞集合"）。
- 依赖关系（运行期）：
  `资产事实(A) × 知识(K) → 疑似资产-漏洞集合(M) → 定向扫描确认(S) → 候选链(R) → 验证(V) → 报告(P)`，其中 K 可随时更新并触发 M 重算。

### 1.2 "整体流程全部通过多智能体来完成"需要改成"编排与解释由智能体、判定与放行由程序"

这条如果不改，会直接冲突：报告的硬性要求是版本匹配、授权范围、状态转换、安全条件由**规则**判定，模型输出只能作为分析建议（报告 336、342、693）；工具执行成功的返回码、模型的高置信度都**不能**作为漏洞成立依据（报告 714）。

如果全部交给智能体：
- 利用链推理无法做 golden test（同输入同规则必须同结果）——方案 P4 的出口条件会直接失败；
- "关闭模型后确定性路径仍可生成"（方案 P4 出口）无法实现；
- 软著演示会不稳定，同一个靶场两次跑出不同结论；
- 报告里写的"确定性规则"在评审时无法自证。

建议措辞与分工（写进方案）：
- **智能体负责**：规划、检索、非结构化解析、关联假设、能力选择、结果解释、任务调整建议、报告叙述。
- **程序负责**：版本/CPE/PURL 比较、去重与合并、授权与范围校验、状态转换、证据判定、链路剪枝边界、评分计算、审批放行。
- 智能体提出的结论一律先落 `candidate`，由规则/证据/人工转成 `confirmed`。

### 1.3 反馈回路（你的第 5 条）需要限次与依赖追踪，否则会无限循环

"发现缺少的资产和漏洞信息 → 反馈给前面的环节 → 再推理 → 再测试"是报告 719 的"局部重算"要求，方向完全正确。但必须补三件事：

1. **反馈事件表**：记录谁触发、影响哪些事实/候选链/任务、重算了什么（这是"报告可追溯"的一部分）。
2. **依赖追踪**：候选链和验证任务要记录它依赖哪些事实版本，才能只重算受影响子图，而不是全量重跑。
3. **限次与停止条件**：每个研究任务设定最大反馈轮次（如默认 3 轮）、每轮增量信息门槛（没有新增有效信息就停）、以及超出后转人工确认。否则多智能体一定会出现"反复补证—反复测试"的成本失控。

### 1.4 被动流量分析是本轮最大的新增子系统，需要先收紧首版范围

"以流量分析为主、得到可达网络中的资产拓扑"在报告里对应 4.1.1 多源被动发现（Zeek 等）。但 PentAGI 现在**完全没有抓包/流量分析能力**（26 张表、42 个工具里都没有），这是全新子系统。

建议首版明确为：
- **输入形式**：pcap 文件、Zeek 日志（conn/dns/http/ssl 等）、Suricata eve.json、以及防火墙/资产台账 CSV 导入。
- **处理方式**：离线解析（R0/R1 级），不承诺首版做镜像口实时抓包。原因是实时抓包在演示环境需要 SPAN/TAP，且属于你已说"靶场独立环境、暂不处理"的范围。
- **输出**：资产观测事件、通信关系（源-目的-端口-协议-次数-首末时间）、由观测归并出的资产与拓扑边、以及"信息缺口清单"交给主动补全。
- 实时抓包作为后续独立能力，接口先留出（例如 `capture_source` 抽象：文件源 / 实时源）。

### 1.5 外部知识采集：源、频率、存储、许可需要写细，否则会因为数据量做不下去

你要求"定时收集 CVE、CWE、MITRE ATT&CK 及其他威胁情报"以及"用户上传国标/规定等资料"。现状：PentAGI 没有调度器，也没有漏洞知识结构。

建议明确：

| 来源 | 方式 | 频率 | 许可/限制 |
|---|---|---|---|
| NVD (CVE) | REST API 增量（`lastModStartDate`） | 每日一次，可手动触发 | 免费但建议申请 API key（否则限速很低） |
| CISA KEV | 官方 JSON | 每日 | 公开 |
| MITRE ATT&CK | STIX/TAXII 或 GitHub 发布包 | 按版本更新 | 公开 |
| CWE | XML/CSV 发布包 | 按版本更新 | 公开 |
| 厂商安全公告 | 定向网页采集 + 人工上传 | 按需 | 遵守站点条款与 robots |
| CNVD | 视许可与接口条件 | 待定 | **需先确认许可，不承诺无限制采集** |
| 用户上传（国标、行业规定、设备资料） | 上传 + 解析 | 手动 | 需登记来源、密级、可调用模型范围 |

存储分工（直接决定本机能否跑得动）：
- **CVE/CWE/ATT&CK 存结构化事实**（PostgreSQL 表），**不要**把几十万条 CVE 全部向量化。16GB 机器上把全量 NVD 做 embedding 会直接把内存和磁盘吃满，而且检索质量并不比结构化过滤好。
- **向量化只用于文档类**（国标、厂商公告、设备手册、用户上传资料），按项目/任务范围过滤。
- 文本检索用现有 `pg_trgm` 全文能力兜底。
- 采集必须保留原始快照（原始 JSON/HTML 文件 + 哈希 + 采集时间 + 解析器版本），解析结果不覆盖原件。

### 1.6 知识图谱：上 Neo4j，但必须明确"谁是事实源、谁写图"

按 D12，图谱直接用 Neo4j 并全部可视化。这里有一个必须先定的架构点，否则后期会返工：

**Neo4j 是投影，不是事实源。** 采用报告 540-545 的三层存储：
- 结构化事实层（PostgreSQL）：漏洞、影响范围、条件、检测特征、设备约束、资产、证据——所有判定与状态都在这里；
- 图谱关系层（Neo4j）：由事实层经 outbox 确定性写入的关系边，用于可视化与多跳查询；
- 证据层（文件 + 哈希）：原始材料。

**为什么不让 Graphiti 直接当领域图谱。** PentAGI 现有的 Graphiti 集成是"时序知识图谱 + 大模型抽取"（episode/实体提取），适合存智能体消息与经验记忆，**不适合**作为
"漏洞—组件—版本范围"这种需要精确、可复算、可 version 的类型化关系。建议：

| 用途 | 存储 | 写入方式 |
|---|---|---|
| 领域图谱（资产/组件/漏洞/条件/链/证据关系） | **Neo4j 自有 schema**（节点标签 + 关系类型由我们定义） | 由 PostgreSQL 经 outbox **确定性写入**，可用 Cypher 复算 |
| 智能体消息/经验记忆 | 沿用现有 Graphiti（可选）+ pgvector | 现有逻辑，保持不变 |

关系类型首批（对应报告 535 的三条关系链，外加链与证据）：
`HAS_INTERFACE` / `RUNS_SERVICE` / `USES_COMPONENT` / `AFFECTED_BY`（组件—漏洞，带版本范围）/ `HAS_CONDITION` / `DETECTED_BY`（漏洞—检测特征）/ `DEVICE_USES_PROTOCOL` / `CONSTRAINED_BY`（设备—检测约束）/ `CHAIN_NODE` / `SUPPORTED_BY`（节点—证据）。

**资源代价与对策（本机 16GB）。** Graphiti overlay（Neo4j 4G + Graphiti 2G + 4G shm）叠在 Postgres、应用、容器上会紧张，对策：
- 只起 Neo4j（关掉 Graphiti 服务或仅按需启动），Neo4j 堆内存按 2–3G 配置；
- 项目/任务级图谱可用 `project_id` 属性过滤，不做多库；
- 图谱可由事实层随时重建，因此不纳入备份关键路径（与 D4 的"靶场独立环境"不冲突：图谱在本机，靶场在外网）。

**可视化**：漏洞收集页与利用链页共用同一个关系图组件（节点形状按类型、边方向表示关系语义、节点旁带状态文本标签，不用颜色单独表达状态——报告 759 的要求），支持按起点/目标/安全域/风险筛选，以及"证据可达"跳转。

### 1.7 "用户自己添加和配置 Skill / MCP"是新功能，需要分期和安全边界

现状：PentAGI 有 42 个内置工具注册表、按角色的工具集，以及一个**已存在于 API/数据库但从未生效**的外部函数机制（`tools.Functions`，见 `pkg/tools/tools.go:443-444` 只赋值不读取）。没有 MCP 客户端，也没有 Skill 管理界面。

建议分期：
1. **第一步（随 A 阶段地基一起做，成本低）**：能力注册表 CRUD —— 名称、说明、参数 JSON Schema、证据类型、风险等级、适用阶段/角色、审核状态；内置工具 + 用户自定义 HTTP 能力（把现有 `Functions.Function` 接上，让"外部函数"真正生效）。
2. **第二步（随 G 阶段验证闭环一起做）**：MCP over HTTP/SSE 客户端（容器内出网访问 MCP 服务），把 MCP 工具映射为注册表条目。
3. **第三步（最后做）**：stdio 型 MCP（需要拉起子进程）——必须做进程隔离与审批。

安全边界（必须写进方案）：
- 用户自定义能力一律带**风险等级与审核状态**，未审核不得进入 R2 以上流程；
- 所有能力（含 MCP）都必须经过同一道策略网关，不能绕过；
- 用户上传的知识资料、MCP 返回内容都按**外部不可信数据**处理，不能修改授权策略、任务状态或触发新的工具调用（报告 677）；
- 能力变更生成新版本，正在运行的高风险任务不自动继承扩大后的权限。

### 1.8 三种入口（分步页面 / 自动执行 / 交互助手）共用一个阶段状态机

这是解决我上一轮提的"阶段与 Flow 之间没有映射"的关键。按 D6 + D9，入口有三个，但状态只有一份：

- 一个**研究任务（research_task）**是根对象，下挂五个**阶段（stage）**，阶段之间是固定 DAG + 反馈边。
- 阶段状态、输入输出契约、证据引用全部存 PostgreSQL，**阶段完成由产物与校验决定**，不由模型说"完成"决定。
- 每个阶段执行时创建一个 PentAGI **Flow**，Flow 只作为"执行容器与实时轨迹"，阶段页读取阶段状态与产物。
- **分步页面模式**：用户在某页点"执行本阶段"，只跑该阶段（前置未满足时提示缺什么、给补充入口）。
- **自动执行模式（类 PentAGI 自动 Flow）**：配置范围与方法后启动，编排器按 DAG 依次触发阶段；遇到人工点（授权确认、R2/R3 审批、证据冲突、反馈轮次超限）暂停等待。
- **交互助手模式（类 PentAGI Assistant）**：不预先跑全流程，用户在对话里下达意图，助手通过新增的"研究工具"（如 `run_stage`、`list_assets`、`match_vulnerabilities`、`propose_chain`、`request_verification`）逐个驱动阶段，并把每一步写回同一份阶段状态与对象表。助手模式下的每个产出同样要落库、落证据，不能只停在聊天里。

三条入口写的是同一份状态，因此演示时可以从自动模式切到手动接管，也可以用助手补充说明后再继续。**不要**让不同入口各自维护一套状态。

PentAGI 现有的两条链路（自动 Flow 与 Assistant）可以直接复用其执行、消息、订阅、停止/恢复机制，我们只在其上加"阶段编排器"和"研究工具"。

### 1.9 埋点：实验指标暂不做，但两列数据建议现在留

人工介入工时与"有效漏洞覆盖率"事后无法重建。既然软著需要演示、后续也可能要指标，建议只加最小的两处：
- `activity_log`：操作主体、阶段、活动类型、开始/结束时间、关联对象 ID（用于将来统计人工分钟数）；
- `asset_vulnerabilities` 上的 `evidence_threshold_met` 与 `ground_truth_ref` 标记（用于将来统计有效覆盖率）。

成本很低，不做的话以后只能重做数据链路。

### 1.10 精度与准确度的保障清单（按 D11，"做对"具体指这些）

"精度"不靠更大的模型，靠下面这些**程序侧**的动作。以下每一项都要求实现 + 冻结夹具，缺一项精度就会塌：

| 环节 | 精度保障动作 | 不做的后果 |
|---|---|---|
| 资产归并 | 强标识优先（序列号/云资源 ID/校验过的 MAC）；候选评分；硬冲突禁止自动合并；保留成员观测、算法版本、分项得分，可拆分回滚 | 同设备被拆成多个资产或不同设备被合并，后续全部结论失真 |
| 版本判定 | CPE 2.3 / PURL 归一 + 官方词典匹配；SemVer / Debian / RPM 三个比较器；区间开闭、epoch、revision、预发布都按各生态规则；无法判定时返回 `unknown` 而不是猜 | 版本比较做错 = 系统性误报/漏报，是漏洞匹配最大的错误来源 |
| 候选集合 | 只按"产品组件 → 版本范围 → 已有适用条件"逐步收敛；证据冲突保留 `conflict`；每次筛选记录资产画像版本与知识版本 | 把"可能相关"直接当成"疑似存在"，后面全是噪声 |
| 扫描判定 | 三态（已确认/未检出/待确认）必须以**检测覆盖快照**为前提；`not_detected` 只在检测条件完整执行时产生；证据按来源独立性去重（同一原始来源的派生记录不重复计数） | 把"没扫到"当成"不存在"，或把同一告警重复计数 |
| 利用链 | 条件/效果词表 + 规则校验；有界搜索（深度/分支/未知数/时间上限）；越权与安全域方向冲突立即剪枝；未知条件不得当作成立；可信度与风险分项分开且权重版本化 | 链不可复算，评审无法验证，"关模型仍能出路径"无法证明 |
| 验证结论 | 七类结论按规则判定；工具返回成功、退出码、模型置信度只作中间信息；前中后基线 + 目标侧直接观察才算证据 | 把执行成功当成漏洞成立，这是最严重的一类错误 |
| 证据链 | 原文不可变 + SHA-256 + 来源/时间/采集主体/解析器版本；解析结果不覆盖原件；证据↔结论双向可追溯 | 报告结论无法回溯，等保证据链不成立 |
| 报告 | 只从已确认的结构化记录装配；缺证据禁止发布；模型只写叙述且必须带证据 ID | 出现无来源的结论 |
| 输入安全 | 外部资料/工具输出/MCP 返回一律当数据，不得改写策略、状态或触发新调用（报告 677） | 提示注入可导致越权扫描，直接违反零容忍要求 |

**精度的验证方式**（报告要求实验，我们不统计指标，但测试必须有）：冻结夹具 + 黄金测试。
- 版本比较：NVD 公告提取的边界夹具（开闭区间、epoch、预发布、异常输入），三生态各一组；
- 资产归并：同设备多来源、地址复用、硬冲突场景；
- 候选匹配：产品别名、CPE/PURL、缺失版本、冲突来源；
- 利用链：合成图包含单资产、跨资产、环、版本不符、不可达、未知、冲突、越权目标；
- 验证结论：七类结论各一组契约测试；
- 确定性：同一输入 + 同一规则版本 → 同一路径签名；关闭模型仍能生成路径。

### 1.11 "做到最优"的边界：哪些必须一次做对，哪些可以后面加深

按 D13，"能直接做好的就做到最优"。为了不把它变成"永远出不来"，建议这样切：

**不可逆项（第一版就必须做对，后改等于重做）**
1. 对象 ID、状态词表（报告 ↔ 方案 ↔ 界面 ↔ 数据库）、范围与授权模型；
2. 证据模型（不可变、SHA-256、来源与解析版本）；
3. 阶段状态机与阶段↔Flow 映射、任务/阶段/产物的接口契约；
4. 能力注册表 + 策略网关 + 审计（后期插入会把所有执行路径重改一遍）；
5. 版本比较与 CPE/PURL 归一（精度地基）；
6. Neo4j 关系类型定义与 outbox 投影契约（改 schema 要重建全图）；
7. 项目/任务范围过滤（漏了就是数据越权，事后补要全量回查）。

**可逆项（可以先做扎实，再逐步加深）**
- 页面样式、图表细节、文案；
- 单个采集源适配器（先 3 个源，再扩到更多）；
- 单个检测能力适配器（先 Nmap + 只读 HTTP/TLS，再 Nuclei/更多）；
- 报告模板与章节（报告正文可以后配模板）；
- 被动流量的解析器种类（先 pcap/Zeek，再 Suricata/更多）；
- 机器学习增强（按 D3 后置）。

也就是说：**地基按"最优"标准一次到位，纵深按阶段推进**；每个阶段结束时流程都能从头走到尾，但不会因为"先出薄版"而在后面推翻数据模型。

---

## 2. 建议的总体架构

### 2.1 五阶段主流程（页面顺序按你的 D1–D5）

```
(K) 漏洞收集与知识图谱  ← 常驻：外部定时采集 + 用户资料上传 + 知识图谱 + 检索
        │
        ▼
(A) 资产发现（被动流量为主 + 主动最小化补全 / 传统主动发现）
        │
        ▼
(M) 疑似资产-漏洞集合（A × K 匹配：产品组件 → 版本范围 → 服务/配置条件）
        │
        ▼
(S) 定向漏洞扫描（共享检测项 + 漏洞专用检测 → 资产-漏洞状态：已确认/未检出/待确认）
        │
        ▼
(R) 利用链推理（事实快照 → 有界路径生成 → 剪枝 → 可信度/风险分项 → 候选链）
        │
        ▼
(V) 多 Agent 渗透测试验证（验证任务图 → 能力选择 → 受控执行 → 证据判定 → 七类结论）
        │
        ├── 反馈：缺失资产/漏洞/条件 → 回 A / K / M / R（限次，见 §1.3）
        ▼
(P) 报告输出（结构化装配 + 模型叙述 + 证据附录；模板后续给）
```

界面按「漏洞收集与知识图谱 / 资产发现与漏洞扫描 / 利用链推理 / 渗透测试 / 报告输出」五页组织，M 与 S 合并在一页（左：疑似集合与确认状态；右：扫描计划与证据）。

### 2.2 阶段 ↔ PentAGI 执行对象映射（冻结项）

| 研究侧对象 | PentAGI 对象 | 写入方 | 说明 |
|---|---|---|---|
| `research_task` | 新表（`user_id` 负责人） | API | 范围与授权挂在它下面 |
| `stage`（5 个 + 子阶段 M/S） | `flow`（每次阶段执行一个） | 编排器/用户 | 阶段状态存 PostgreSQL，绝不由模型直接写 |
| 阶段内步骤 | `task` / `subtask` | 现有规划器 | 只读投影到阶段页 |
| 执行轨迹 | `toolcall` / `msglog` / `termlog` / `screenshot` | 现有执行器 | 直接复用 |
| 证据 | `flowfiles` 二进制 + `evidence` 表 | 确定性服务 | 新增 SHA-256 与不可变版本 |
| 实时状态 | 现有 GraphQL 订阅 | 现有发布器 | 阶段页订阅阶段/工具/审批状态 |

重要约束：**验证任务图与依赖边不能以 PentAGI `subtask_id` 为键**——现有 `RefineSubtasks` 会删除并重建计划行（`pkg/controller/subtasks.go:97-139`），id 会变。全部用新业务表的稳定 ID。

### 2.3 智能体与程序的分工（回应 §1.2）

| 环节 | 智能体做 | 程序做 |
|---|---|---|
| 漏洞收集 | 文档解析、实体抽取、来源冲突解释、匹配建议 | 结构化入库、去重、版本比较、关系边写入 |
| 资产发现 | 被动观测归并建议、缺口分析、主动补全方式选择 | 归并算法、可达性判定、范围与频率校验、执行放行 |
| 候选匹配 | 别名/组件归一建议 | CPE/PURL 匹配、版本区间判断、状态机 |
| 扫描 | 检测技能选择与排序、结果解释 | 任务依赖、共享检测项合并、证据判定、状态判定 |
| 利用链 | 语义条件解释、未知条件补全建议 | 有界搜索、剪枝、去环、分项评分 |
| 验证 | 任务调整建议、失败归因、结论解释 | 依赖调度、策略网关放行、证据判定、七类结论 |
| 报告 | 叙述撰写 | 字段装配、引用校验、快照与发布门禁 |

### 2.4 自动执行模式与交互助手模式的设计（D9）

两种模式不是两套系统，而是同一编排器的两种"驱动源"：

| | 自动执行模式 | 交互助手模式 |
|---|---|---|
| 复用 PentAGI 的 | 自动 Flow（`primary_agent` + generator/refiner/pentester 循环） | Assistant（`assistant` 角色 + 消息链，无任务分解） |
| 启动方式 | 任务中心配置范围与方法后一键启动 | 研究任务里直接对话下达意图 |
| 阶段推进 | 编排器按 DAG 自动触发；到人工点暂停 | 助手调用研究工具逐个驱动阶段，可跳步、可重跑 |
| 人机交互 | 只在审批/冲突/超限时介入（`AskUser` 屏障） | 全程可追问、可补充、可纠正 |
| 产出要求 | 每阶段写阶段状态 + 产物 + 证据 | **完全一致**：助手模式下每个产出同样落库落证据 |
| 适合场景 | 软著演示、批量跑、无人值守 | 探索式分析、演示讲解、调试与补充信息 |

实现要点：
1. 新增一组"研究工具"（`run_stage`、`stage_status`、`import_artifact`、`match_vulnerabilities`、`propose_chain`、`request_verification`、`submit_report` 等），注册到现有工具注册表；
2. 这些工具在自动模式与助手模式下**同一套实现**，只是调用者不同（编排器 vs 助手）；
3. 助手模式创建的研究任务同样有 `stage` 行，状态机不允许"聊天里说完成就是完成"；
4. 自动模式的停止/恢复复用 P0 的持久作业与 `flow_jobs` 机制（单流程 FIFO、跨流程 4 并发、重启恢复），阶段作业作为新的 job kind；
5. 两种模式共用同一套订阅（阶段状态、工具调用、审批、反馈事件），前端五个页面在两种模式下都是同一套组件。

---

## 3. 复用清单（直接复用 / 接线后复用 / 必须新建）

**直接复用**
- Flow / Task / Subtask 执行、消息链、摘要与上下文压缩
- 13 个可配置模型角色、按角色模型配置、自定义兼容接口
- 42 个内置工具 + 每角色工具集、Docker 每流程容器、终端/文件/截图/浏览器
- 多搜索源与网页抓取（漏洞收集的外部采集可直接用）
- 知识库文档 + pgvector 检索 + 资源库 + 模板 + 提示词覆盖表
- `flow_jobs` 持久作业（单流程 FIFO、跨流程 4 并发、重试、重启恢复）
- GraphQL/订阅/Apollo 前端框架、中文文案表 `uiText`、现有报告页与 MD/PDF 导出
- 交互助手模式（人工介入与补充说明）

**接线后复用（已有骨架，需要打通）**
- `tools.Functions.Disabled` / `Functions.Function`：能力白名单 + 外部 HTTP 能力（当前只存不读）
- 每角色工具集：作为"阶段/角色能力白名单"的落点
- 单一工具分发点 `customExecutor.Execute`（`pkg/tools/executor.go:242`）+ 容器执行点 `pkg/docker/client.go:907`：策略网关与审计
- Neo4j / Graphiti 集成（`pkg/graphiti`，默认关闭，`docker-compose-graphiti.yml` 已存在）：按 §1.6 改为"Neo4j 自有 schema 承载领域图谱，Graphiti 仅用于智能体记忆"
- pgvector：加项目/任务范围过滤

**必须新建**
- 项目/范围/授权/策略版本
- 资产台账、观测、归并、可达性、服务/组件
- 漏洞知识（CVE/CWE/ATT&CK/公告）、影响范围、条件/检测特征
- 候选集合与资产-漏洞状态机
- 候选利用链、节点/边、条件、评分
- 验证任务图、审批、执行记录、证据（SHA-256、不可变）
- 能力注册表、策略网关、R0–R4
- 阶段编排器、反馈事件、埋点
- Neo4j 领域 schema + outbox 确定性投影 + 全量重建脚本 + 关系图前端组件
- 被动流量解析（pcap/Zeek/Suricata 导入）
- 五个业务页面 + 任务中心 + 审批队列 + 审计页 + 能力/Skill/MCP 管理页
- 报告快照与发布门禁

---

## 4. Skill 与 MCP 开放配置（对应你的第 7 条）

配置对象建议收敛为四类，避免"什么都能配"导致无法验收：

| 对象 | 配置内容 | 生效范围 |
|---|---|---|
| 智能体 | 角色启停、模型、提示词、可用能力集合、最大并发 | 全局默认 + 按项目覆盖 |
| 能力（内置工具 + 自定义 HTTP + MCP 工具） | 名称、说明、参数 Schema、证据类型、风险等级、审核状态、超时/限频 | 注册表全局，按阶段/角色授权 |
| 知识源 | 类型（API/网页/文件）、地址、凭据、频率、解析器 | 全局，按项目启用 |
| 报告模板 | 结构字段、章节、脱敏规则 | 全局 + 按项目选择 |

一句话：**用户能加"能力"和"知识源"，但不能加"绕过网关的路径"**。

---

## 5. 前端页面清单（首版）

| 页面 | 关键内容 | 复用 |
|---|---|---|
| 任务中心 | 建任务、范围/授权配置、五阶段进度、一键启动、进入各阶段、反馈轮次与待人工处理事项 | 新建（借鉴现有 Flow 列表/详情布局） |
| 漏洞收集与知识图谱 | 知识源管理、采集任务与运行记录、资料上传与解析、漏洞列表与详情、知识图谱视图、检索、按当前资产重新匹配 | 复用知识库/资源/模板页与图谱渲染（新建） |
| 资产发现与漏洞扫描 | 导入（pcap/Zeek/Suricata/台账/Nmap XML）、被动观测与拓扑、资产详情与归并确认、疑似资产-漏洞集合、扫描计划与执行、证据与状态 | 新建（复用 Flow 终端/消息组件展示执行） |
| 利用链推理 | 发起分析、路径列表、关系图、节点条件与证据、未知项、提交验证 | 新建 |
| 渗透测试 | 验证计划与任务图、执行控制、实时活动、助手介入、审批、结果与七类结论、反馈给前序环节 | 复用 Flow 详情/助手/审批（新建） |
| 报告输出 | 选择任务与范围、生成中文报告、引用证据、导出 | 复用现有报告页与导出 |
| 审批队列 / 审计 / 能力与 MCP 配置 | 审批处理、审计查询、Skill/MCP 注册与审核 | 新建（能力页可挂在设置区） |

现有页面（Flow 列表/详情、助手、知识库、资源、模板、模型服务、提示词）全部保留入口，不删不改语义。

---

## 6. 修订后的实施路线（地基一次做对，纵深分阶段推进）

按 D11/D13，路线不再分"薄版/厚版"，而是：**先把不可逆的地基按最优标准做完，再按阶段把每个业务环节做全**。每个阶段结束时，流程都能从第一页走到最后一页。

| 阶段 | 目标 | 内容（做完的标准） | 出口 |
|---|---|---|---|
| **A. 契约与地基** | 一次做对，后续不再改模型 | ① 对象 ID/状态词表/范围与授权模型冻结；② 证据模型（不可变、SHA-256、来源与解析版本）；③ 阶段状态机 + 阶段↔Flow 映射 + 阶段接口契约；④ 能力注册表 + 策略网关（含 R0–R4 判定）+ 审计写入点；⑤ Neo4j 领域 schema + outbox 投影 + 重建脚本；⑥ 项目/任务范围过滤（含跨项目负向测试）；⑦ 版本比较与 CPE/PURL 归一（三生态夹具）；⑧ 迁移与回滚演练 | 契约表评审通过；夹具测试全绿；空跑一遍阶段状态机（无模型也能推进阶段） |
| **B. 五页骨架 + 任务中心** | 界面结构定稿 | 任务中心 + 五个业务页面 + 共享任务上下文 + 证据侧栏 + 路由/导航/面包屑/中文文案 + 一键启动入口（先接骨架） | 跨页切换、刷新、深链接不丢范围；页面只显示真实产物 |
| **C. 漏洞收集与知识图谱** | 知识底座 | NVD 增量 / CISA KEV / ATT&CK / CWE / 厂商公告采集 + 用户资料上传解析 + 原始快照与版本 + 结构化入库 + Neo4j 关系写入 + 图谱可视化 + 检索（含项目范围） | 定时任务可运行可观测；图谱三条关系链可视化；知识修订可追溯、解析不覆盖原件 |
| **D. 资产发现与候选匹配** | 事实底座与精度 | pcap/Zeek/Suricata/台账/Nmap XML 导入；观测归并（强标识 + 评分 + 硬冲突 + 可拆分）；拓扑视图；可达性；信息缺口清单与主动最小化补全；CPE/PURL 匹配 + 版本范围筛选 → 疑似资产-漏洞集合 | 同一资产多来源归并正确且可拆分；重复导入不增行；冲突字段并列可见；越权目标无法进入主动阶段 |
| **E. 定向扫描与状态判定** | 检测闭环 | 共享检测项 + 漏洞专用检测；Nmap 低频识别 + 只读 HTTP/TLS 核验能力适配（走网关）；检测覆盖快照；三态判定（已确认/未检出/待确认）；证据回填与共享复用 | 无证据不出结论；`not_detected` 只在覆盖完整时产生；跨页证据可回溯 |
| **F. 利用链推理** | 核心创新点 | 事实快照 + 条件/效果词表 + 规则校验 + 有界搜索 + 剪枝去环 + 可信度/风险分项（版本化权重）+ 图谱化展示 + 提交验证 | 合成图 golden test 通过；相同输入同规则同路径签名；关模型仍可出路径；超限返回截断原因 |
| **G. 多 Agent 受控验证与反馈** | 闭环与安全边界 | 验证任务图 + 能力选择 + 策略网关放行 + 审批队列（R2 单审、R3 双审）+ 七类结论 + 前中后基线与恢复检查 + 反馈事件与限次重算 | 工具成功不等于漏洞成立；越权/禁止动作/服务中断测试为 0；legacy 通路无法从研究任务入口调用 |
| **H. 报告与双模式收口** | 交付与演示 | 报告快照（冻结数据修订/文件哈希/规则与模板版本）+ 发布门禁 + 模板化输出 + 断模型重建验证 + 自动执行模式编排器 + 交互助手模式研究工具 + 审计页 + 软著材料（功能清单、截图、说明书） | 一键跑完五阶段遇审批暂停；助手模式全程可驱动且产出落库；断模型可重建报告 |

阶段顺序上的两点说明：
- **A 阶段最不能省**。它对应我上一轮指出的三个阻塞项（阶段映射、能力隔离、判定与证据规则），也是 D11"精度优先"的实际含义：这里省下的时间，后面要成倍还。
- **C 与 D 谁先做不影响精度，但影响演示顺序**。建议按上表先 C（知识底座）再 D（事实底座），这样"漏洞收集"页面先有内容，符合你的流程顺序；若更想早点看到资产拓扑，也可 D 先 C 后。

---

## 7. 仍需你确认的决定点

已在本轮确定、不再询问的：一键模式仿 PentAGI（自动 + 交互助手）；审批按需加入；图谱上 Neo4j 并可视化；精度优先、地基一次做对。

还剩这些需要你定，其中 1–3 会直接影响 A 阶段的设计：

1. **Neo4j 与 Graphiti 的分工**：是否接受"Neo4j 用我们自定义 schema 承载领域图谱（确定性写入、可重建），Graphiti 只用于智能体消息与经验记忆"？如果要求 Graphiti 直接承载领域图谱（大模型抽取实体关系），那么"同输入同结果"的可复算性会下降，需要评审确认接受。
2. **Neo4j 常驻还是按需启动**：本机 16GB，我建议 Neo4j 常驻（堆 2–3G）、Graphiti 服务按需启动；若你希望两者都常驻，需要相应减少其他容器或降低并发。
3. **交互助手模式与自动模式是否共用同一个研究任务**：我建议共用（同一 `research_task` 下同一批阶段行，只是驱动源不同），这样中途接管、演示切换都自然。若你希望两者是两种独立的"任务类型"，请说明。
4. **审批的默认策略**：R0/R1 预授权直接执行（不弹审批），R2 单次审批，R3 两名不同审批者——是否按此默认？还是每个项目都要单独配置？
5. **知识源许可与凭据**：NVD 是否已有 API key？CNVD 是否已确认可用？这决定 C 阶段的采集范围与频率。
6. **被动流量的优先级**：先做 pcap 文件，还是先做 Zeek 日志？（我建议先 Zeek 日志，结构化程度高、解析成本低，pcap 作为补充。）
7. **自定义能力（Skill/MCP）的生效方式**：全局注册、按项目启用，是否需要"审核通过后才可用于 R2 以上"？我建议都需要审核，避免演示时误用未经验证的能力。
8. **软著材料**：是否需要我从 A 阶段开始就按软著要求整理功能清单、界面截图与操作说明（避免最后补材料）？

---

## 8. 与报告口径的差异说明（写进方案，避免评审对不上）

| 项 | 报告口径 | 本方案口径 | 说明 |
|---|---|---|---|
| 主流程 | 扫描—收集—推理—利用 | 收集（常驻）→ 发现/扫描 → 推理 → 验证 → 报告 | 页面顺序按业务习惯前移收集；运行期依赖仍是"资产先于匹配"；报告 294 本身就允许收集为持续服务 |
| 知识产权与图谱 | 结构化事实 + 图谱关系 + 证据三层协同存储 | 完全对齐：PostgreSQL 事实源 + Neo4j 领域图谱投影 + 文件与哈希证据层 | 按 D12；图谱可视化，且可由事实层重建 |
| 使用方式 | 报告第 7 章为多模块界面 + 关键交互流程 | 五阶段业务页面 + 任务中心 + 三种入口（分步/自动/助手） | 按 D6/D9；自动与助手模式复用 PentAGI 现有两条链路 |
| 机器学习 | 资产分类、历史预测、误报过滤 | 弱化：规则+统计+置信度，保留模型版本字段 | 按 D3；后续需要时再启用，报告里不宣称已实现 |
| 指标体系 | 15% 两项主指标 + 实验 | 暂不实现，仅保留埋点与夹具测试 | 按 D1/D11；精度靠夹具与黄金测试，不靠指标统计 |
| 靶场 | GNS3/Vulhub/Juice Shop/DVWA/Metasploitable | 独立环境，本机不做 | 按 D4；本机夹具用导入数据 |
