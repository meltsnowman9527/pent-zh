# 《PENTAGI_RESEARCH_MODIFICATION_PLAN.md》评审意见

> 历史讨论留档：保留原文观点与决策过程。后续实施以 [开发方案](docs/research-platform/PLAN.md) 和 [开发契约](docs/research-platform/CONTRACTS.md) 为准；本文中的建议、待确认事项及完成声明不代表当前代码状态。

评审人：DSH 智能体 · 日期：2026-09-15 · 基线：分支 `codex/research-development`，HEAD `f4e300a`

## 依据材料

- 被评审方案：`PENTAGI_RESEARCH_MODIFICATION_PLAN.md`（398 行）
- 中期研究报告：`tmp/midterm-report-extracted.md`（914 行，下文引用其行号）
- 报告需求清单（逐条抽取）：`tmp/requirements-checklist.md`
- 当前进度与部署：`IMPLEMENTATION_STATUS.md`、`DEPLOYMENT.md`
- 实际代码：`backend/pkg/tools`、`backend/pkg/controller`、`backend/pkg/providers`、`backend/pkg/flowfiles`、`frontend/src`

---

## 1. 总体结论

**方向正确、与报告主线一致，明显优于"照报告从零搭八个模块"的方案。但按现有文字直接开工 P1 不安全**：有 3 项只在策略层做了承诺、没有运行期设计；有 6 处缺口需要在 G1 之前定案；报告自身的两项承诺被静默丢弃。

| 维度 | 评价 |
|---|---|
| 与报告技术路线的一致性 | 好。四环节主线、五层架构、控制面与执行面分离、确定性规则优先于模型判断、证据优先、R0–R4 |
| 对 PentAGI 现有优势的利用 | 部分。Flow、智能体、工具、Docker、pgvector 的复用层级正确，但方案打算新建的几个子系统其实仓库里已有对应机制 |
| 可执行性 | 中。数据对象与状态机定义清楚；但与 PentAGI 执行模型的对接完全没有定义 |
| 16GB 单机部署的现实性 | 有节制（不拆微服务、不强制 Neo4j），但 P1 实际是两个批次的工作量，靶场与夹具排得太晚 |
| 返工风险 | 高，集中在三处：阶段与 Flow 的映射、能力隔离、以及被丢弃的评测与机器学习范围 |

**一句话结论**：保留方案骨架，在 G1 之前补齐 §3.1–§3.3 三个阻塞项和 §3.4–§3.9 六个主要缺口，并按 §4 复用现有机制而不是另起炉灶。§8 给出了可直接落到方案里的修改清单。

---

## 2. 方案做得对的地方（应当保留）

1. **权责划分正确。** 版本匹配、授权范围、状态转换、安全条件由确定性规则负责，模型只产出候选（报告 336、342、693）。方案 §3.3、§4 表达一致，并明确"运行成功只更新执行事实，不直接更新漏洞结论"。这是报告中最容易被违反的一条，方案处理正确。
2. **存储选型符合本机现实。** 以 PostgreSQL 为唯一事实源、pgvector 只做检索索引、图谱作为可重建投影（§3.1）。报告 536 本身就说明图谱只提供知识依据，因此暂不部署 Neo4j 是站得住的；但必须配一个伴随决定，见 §3.7。
3. **复用姿态正确。** §2.1 保留了 Automation 与 Assistant 双模式、专业智能体分工、按角色配置模型、摘要与记忆、多搜索源、Docker 工具隔离、任务文件、模板、实时消息与 MD/PDF 导出；§2.2 又明确禁止把既有缺陷当特性保留。这个框架是对的。
4. **范围诚实。** §7 用纵向切片 A–D 取代"做完一个大模块再开下一个"，§8 定义了 G1–G5 决策门，并写明"未通过时允许做什么"。很少有方案能做到这一点。
5. **出口条件针对失败路径而非顺利路径。** P1 的跨项目负向测试；P2 的越界目标、DNS 变化、过期审批、参数篡改；P3 的别名、CPE/PURL、SemVer/Debian/RPM 边界夹具；P4 的含环、不可达、未知、冲突、越权的 golden graph；P5 对七类结论逐类契约测试。这些都能对回报告 §3.3、§3.4、§3.6、§4.2–4.3、§6.4 与表 6.4。
6. **P0/P0A 的代码层结论可核验。** `flow_jobs` 迁移 `20260913_180000_flow_jobs.sql`、`pkg/controller/flowjobs.go`（单流程 FIFO、`flowJobMaxConcurrency=4`、3 次重试、重启恢复），以及 12 个生命周期/竞态测试。中文化是真实机制而非说法：`frontend/src/locales/zh-CN.ts` 配合 `uiText(...)`，约 1,498 处调用、154 个文件，另有后端错误码到中文的映射（`frontend/src/lib/errors.ts`）。但有两点必须记录而不是默认成立：方案引用的 p95 数值和 2026-09-14 之后的测试总数在 `build/` 下**没有产物**；真实 Postgres 作业套件在没有 `PENTAGI_TEST_PG_DSN` 时会静默跳过（`pkg/database/flowjobs_pg_integration_test.go:28-30`）。因此方案第 246 行的 P0 收口条件应改为"附重新运行的产物"，而不是引用结论。
7. **幂等键 + outbox + 软删除 + 证据不可变版本**（§3.1）对一个以可追溯性为核心价值的系统来说是正确的契约。

---

## 3. 方案尚不能直接落地的地方（按严重程度排序）

### 3.1 阻塞项一：新业务对象与 PentAGI 执行模型之间没有映射

方案定义了一套完整对象模型（`research_projects`、`assets`、`candidate_chains`、`verification_tasks` 等）和多页面 UI，并说"Flow 复用为执行详情"（§首版页面组织）、"在现有主代理上增加阶段状态与任务路由"（§4）。但它从未说明**运行期的"阶段"到底是什么**。具体未决项：

- 一个研究任务是一个 Flow 并把阶段作为 PentAGI 的 `task`，还是每个阶段一个 Flow？
- 阶段门禁由谁执行——LLM 规划器（generator/refiner）还是应用层状态机？
- "阶段完成由实际产物和必要校验决定"如何与"智能体输出 `done` 就结束"的 Flow 对接？
- 阶段作业如何与 P0 的作业执行器（单流程 FIFO、跨流程最多 4 并发）相互作用？
- 报告的业务对象（`ChainNode`、`Condition`、`Evidence`）由智能体循环写入，还是只由确定性服务在循环结束后写入？

另有两个代码事实约束了答案。第一，**PentAGI 根本没有依赖模型**：`subtasks` 没有边，执行是按 `subtasks ORDER BY id ASC` 的顺序 FIFO 循环（`pkg/controller/task.go:302-329`、`backend/sqlc/models/subtasks.sql:48-55`），前序结果传给后续步骤只靠提示词渲染（`pkg/providers/helpers.go:224-242, 702-762`）。所以方案里的 `TaskDependency`／验证任务图是全新工作，不是扩展。第二，**PentAGI 的 subtask id 是不稳定的**：`RefineSubtasks` 会删除并重建计划行（`pkg/controller/subtasks.go:97-139`），任何以 `subtask_id` 为键的新边表都会在细化后失效。验证任务图必须完全放在新业务表里，用自己的稳定标识。

**建议（G1 交付物）。** 明确并冻结一张映射表：

| 研究侧对象 | PentAGI 对象 | 写入方 | 说明 |
|---|---|---|---|
| `research_task` | 新表，`user_id` 为负责人 | API | 项目范围的根 |
| `stage`（5 个） | `flow`（每次阶段执行一个）+ `research_stage` 行 | 确定性服务 | 阶段状态存 PostgreSQL，绝不交给 LLM |
| 阶段内步骤 | `task` / `subtask` | 现有规划器 | 只作为只读投影展示在阶段页 |
| 执行 | `toolcall` / `msglog` / `termlog` | 现有执行器 | 直接复用为执行轨迹 |
| 证据 | `flowfiles` 二进制 + `evidence` 行 | 确定性服务 | SHA-256（见 §5 R-2） |

同时定下阶段与能力的绑定方式（按 Flow 的 `Functions` 还是按阶段工具集），并定义阶段并发如何与现有 4 路作业执行器共存。

### 3.2 阻塞项二："隔离 legacy 执行通路"只写了要求，没有设计；而它需要的机制目前是死代码

P2 要求："原任意 Shell、动态安装和直接 Docker 通路继续服务旧 Flow 时必须标记为 legacy，并禁止从新研究任务入口调用"，且 legacy 不能绕过网关。三个代码事实：

1. 每个智能体的工具集是**硬编码**的（`backend/pkg/tools/tools.go:1296-1372` 的 pentester 为例，每个角色一个 `Get*Executor`）。因此今天从研究页面创建的 Flow 会无条件获得 `terminal` 与 `file`。
2. `tools.Functions{Disabled []DisableFunction, Function []ExternalFunction}` 已经存在于 REST 模型、数据库列和作业 payload 中（`backend/pkg/server/models/flows.go:57,88`、`flows.go:595`、`pkg/controller/flow.go:432-441`、`pkg/tools/tools.go:39-68`），但解析出来的值**只是被保存，从未在构建工具定义时读取**（`pkg/tools/tools.go:184, 443-444`；全仓库 grep 只有 `cmd/ftester/worker/tester.go:184` 用到 `SetFunctions`）。也就是说，一个"禁用工具"配置已经存在于 API 中，但被静默忽略。
3. 目标范围在工具层没有任何强制；终端工具在每流程容器内执行，而容器统一接入全局配置的 `DOCKER_NETWORK`（或 host 模式），对所有流程容器一视同仁（`pkg/docker/client.go:378-421`、`pkg/config/config.go:52`）。目前没有按任务或按项目的网络策略；不过创建路径本身已接受具名网络，所以"按任务建网"属于扩展而非重写。

**后果**：方案 P2 的出口条件（"legacy 通路无法绕过新任务入口网关"）无法靠配置达成，必须写代码并加负向测试；R3 的"网络隔离"也不能从现有容器模型继承。

**建议。**
- P2 增加一项：把 `Functions.Disabled`（以及新的白名单）接入执行器构建——一个过滤辅助函数加表驱动测试。成本很小，却把已有 API 契约变成报告要求的"能力白名单"。
- 把策略/审批网关做成**单一分发点的装饰器** `customExecutor.Execute`（`pkg/tools/executor.go:242`），该处已经写入工具调用日志（`tclp.PutLog`，第 289 行）并持久化结果。这就是报告 676 的控制面，无需新服务。
- **同时覆盖第二个咽喉点。** 所有容器命令都经 `ContainerExecCreate`（`pkg/docker/client.go:907`），只有三个调用方（`terminal.go:225`、`tools.go:656` 文件同步、`server/services/flow_files.go:1336`——后者执行 `rm -rf --` 删除文件）。只在 `executor.go` 设卡，下一个新增的 `docker exec` 就绕过去了。另需注意：流程容器以 root 运行且挂载了宿主 Docker socket（`docker-compose.yml:206, 211`），网关不能把容器当成自己的权限边界。
- R3 的网络隔离在 G2 定案，三选一：(a) 按任务创建 Docker 网络（较便宜，`client.go:415-421` 已接受具名网络，属于"创建 + 接入"工作）；(b) 为流程容器加防火墙/代理白名单；(c) 独立 runner 容器。不要留作假设。
- P2 出口条件补三条负向测试："研究流程无法调用 `terminal`/`file`"、"R1 能力无法访问白名单外目标"、"参数摘要改变后原审批被拒绝"。

### 3.3 阻塞项三：报告自身的验收内容被丢弃，且没有留下决策记录

方案 §10 与 §7 把实验设计、15% 指标、真值集、人工计时和统计检验放在范围外，P3 又把机器学习资产分类／历史成功率预测放在范围外。但这些来自报告本身：

- 课题预期目标（报告 147）："减少人工介入时间不少于 15%"、"提升漏洞覆盖率不少于 15%"。
- 研究内容 2（报告 151-152）：基于机器学习的资产特征提取与分类、分析历史扫描数据预测未扫描区域资产、误报过滤。
- 第 8 章（报告 793-817）：靶场、两组对照设计、两项主指标的精确计数规则、以及零容忍条件。

这可能是用户明确批准的延后（参见提交 `f4e300a "docs: defer research experiment planning"`）。问题不在于延后，而在于**有两件事现在做很便宜、以后补很贵**：

1. **埋点。** 指标 F1 是"按活动类别累计的人工操作分钟数"，F2 需要按"资产上下文中的漏洞实例"计数并做去重与证据门槛判定。这两者都无法从日志事后重建。建议从 P1 起就加：`activity_log`（操作主体、阶段、活动类型、分钟数、对象 ID）以及 `asset_vulnerabilities` 上的真值／证据门槛标记。这只是几张表几列，却让延后变成可逆。
2. **一个最小且站得住的机器学习／统计组件，或一份书面偏差说明。** 方案目前完全没有机器学习。如果结题或论文/软著依赖研究内容 2，就需要补一个基于规则+统计的分类器并保留置信度与模型版本（报告 379-380 要的正是这种溯源：模型版本、置信度、输入特征），或者由甲方签字确认偏差。

**建议**：增加"范围外但保留接口"小节，列出 (a) 埋点、(b) 真值标记、(c) 可配置场景参数（报告 608/809 要求这些在正式实验前冻结，而不是硬编码），并就机器学习内容取得一句明确决定。

补充一条重要事实：报告通篇几乎**没有数值常量**。除 15%、R0–R4、三态/七态表、八个模块之外，所有深度、分支、时间、阈值都写明是"可配置的场景参数，实验前冻结"（608、809）。方案 §3.6 给的默认值（深度 5、分支 20、未知节点 ≤2、30 秒、路径 ≤100）作为**带版本的默认值**是可以的，但要在配置表中明确它们是可记录、可审计的场景参数，而不是常量。

### 3.4 主要缺口一：报告 §5.2 的 Skill／MCP 能力层没有任何映射

报告把漏洞收集工具规定为三层：智能体决策层／能力接入层（内部 **Skill** + 外部 **MCP**）／功能处理层，并列了八个功能组件（报告 497-514）。方案 §3.5 定义了一套通用适配器接口（`Describe/Validate/Plan/Execute/Cancel/Parse/Health`），但从未提到 Skill 或 MCP。而 PentAGI 已有部分等价物，方案没有点名：

- `registryDefinitions` + `ExecutorHandler` + 工具的 `IsAvailable()` 就是能力注册表。
- `ExternalFunction`（名称 + URL + 超时 + JSON Schema + 上下文列表）就是 HTTP 能力适配器，即 Skill 侧与 MCP 侧注册的天然落脚点。

**建议**：补一张显式映射（Skill → 注册表工具或 `skill` 行；MCP → 带同样注册元数据的能力客户端），并说明表 5.1 的八个组件中哪些复用（采集／解析／治理／存储）、哪些新建（任务调度／关系构建／服务与反馈）。否则"智能化漏洞收集工具"这一交付看起来只是"智能体 + 网页搜索"，而不是报告里的架构。

### 3.5 主要缺口二：界面覆盖相对报告第 7 章存在缺口（审批、审计、权限、仪表盘）

报告要求八个模块（743）、五个权限域（731-741）、审批交互需展示目标/风险/停止条件并可撤回（788）、审计覆盖审批、证据访问与策略变更（770），以及仪表盘的一条硬规则："扫描器告警不得直接统计为已确认漏洞"（745）。方案首版页面是任务中心 + 五个阶段页，其余延后。其中两项延后风险较大：

- **审批队列与审批人身份。** R2 需要绑定目标、参数摘要与有效期的单次审批；R3 需要**两名不同审批者**（表 6.3，报告 686）。目前 PentAGI 只有 admin/user 两个角色，但 `roles`/`privileges` 表结构是真实存在的（`backend/migrations/sql/20241026_115120_initial_state.sql:3-33`，`users.role_id`），所以新增审批人/审计员角色很便宜——但"审批人 ≠ 申请人""R3 需两名不同审批人"这两条规则并不存在，必须实现。应把审批队列页面与身份规则放进 P2，而不是 P5。
- **审计。** `AuditEvent` 在方案 §3 的对象清单里，但既没有页面，也没有写入点（要等到 P5）。建议 P1 就写审计事件（项目/范围变更、导入、合并），P2 写网关判定、审批、证据访问——审计事后补正是等保评审最常见的失分点。

仪表盘与 Agent 管理页面可以继续延后，但要在 §7 明确写出，并补一张"报告八模块 ↔ 首版覆盖"矩阵（方案 §10 已有能力对照表，但没有模块对照表）。

### 3.6 主要缺口三：状态词表需要一张冻结映射表

报告用了多套部分重叠的词汇；方案用的是另一套但兼容的词汇。报告侧：候选筛选四态（表 4.2）、漏洞检测三态（表 4.3：已确认/未检出/待确认）、条件五态、链路衔接四态（完全/部分/不匹配/未知）、验证七类结论（表 6.4），另有任务/判断/约束/数据/整改状态（表 7.2）。方案侧：漏洞五态（`candidate/confirmed/not_detected/conflict/stale`）、条件五态、七类结论。

这是超集、可以自证，但方案自己也警告过口径冲突（§首版目标）。请在 G1 发布唯一一张表：报告状态 ↔ 方案枚举 ↔ 界面标签 ↔ 数据库枚举 ↔ 允许的转换，并声明这是唯一的状态定义处。否则同一问题会在结题报告和界面文案上再次出现。

### 3.7 主要缺口四："不上 Neo4j"需要配一个伴随决定，才能保住知识图谱的说法

§3.1 判断 PostgreSQL + 递归 CTE 足以支撑有界搜索、图谱投影可重建，这是对的。但报告 5.4.2（三条关系链）、6.2（子图提取）与创新点都明确写了知识图谱，前端规格里也有关系图视图（759）。暂不上 Graphiti 没问题；**连图谱建模与可视化一起延后则不行**，因为那样交付物无法演示这个说法。

**建议**：在 P4 把三条关系链落成一等公民的边表（`漏洞–产品/组件–版本范围`、`铁路设备–组件–协议`、`漏洞–条件–检测特征`，外加 `设备–检测约束`），并由 API 渲染链路图（报告只要求图谱提供知识依据，536）。之后若查询需要再接入 Graphiti/Neo4j 作为投影。把这一点写成明确的设计说明。

### 3.8 主要缺口五：外部内容没有被当作不可信输入处理

报告 677 明确写道：外部技术资料、目标返回内容和工具输出"只能作为数据和证据使用，不能直接修改授权策略、任务状态或触发新的工具调用"。方案完全没有提示注入／"数据与指令分离"的控制。而 PentAGI 会把工具结果与网页内容直接回灌进智能体循环，新设计又让智能体生成能力调用请求。这在铁路／等保场景是安全要求，不是可选项。

**建议**：在 §3.7 增加：对进入模型上下文的每段外部片段打来源标记；禁止授权/策略参数与工具调用目标由内容推导；结构化能力请求必须对照冻结的任务范围重新校验；增加夹具测试（一个恶意页面或扫描 Banner 要求智能体去扫描另一台主机，不得产生范围外请求）。

### 3.9 主要缺口六：P5 的"报告快照"与服务端导出并不存在，也没有计入工作量

现在的报告是**可变行**：自动模式报告在任务结束时写成一条 `report` msglog（`pkg/controller/task.go:359-368`），交互助手报告是一条 `report` assistantlog 且**重生成时原地覆盖**（`pkg/reports/assistant_report.go:259-264, 334-351`）。没有报告表、没有修订历史、没有证据绑定、没有发布门禁，而且 **PDF 只在浏览器端生成**（`frontend/src/lib/report/report-pdf.tsx`，`@react-pdf/renderer`）。

P5 要求：草稿→审核中→已发布→复测版，冻结 SQL 修订、文件哈希、规则/提示词/模型/模板版本，证据缺失时阻止发布，并能在**断开模型服务**的情况下重建同样的事实表与证据目录（方案 P5 出口条件，报告 769）。这是新的存储与新的服务端渲染路径。两点影响：

- 现在就决定"导出"留在浏览器端（最简单：服务端返回冻结的 Markdown/HTML 快照，沿用现有客户端 PDF 渲染），还是移到服务端（则需引入 headless Chromium 或 Go PDF 依赖，镜像会变大）。把选择写进 §P5，而不是实现到一半再定。
- "断开模型重建"这条出口条件意味着报告必须由存储的结构化字段装配，这是对快照 schema 的设计约束，不只是一条测试。

---

## 4. 应当更充分利用的 PentAGI 现有机制（附代码证据）

方案 §2.1 的清单方向对，但偏笼统（"保留现有能力"）。下面这些是具体机制，点名之后可以直接从 P1–P5 里减掉工作量。

| # | 现有机制 | 证据 | 可替代方案中的什么 |
|---|---|---|---|
| 1 | 按角色的工具集本身已实现"每个角色只拿到职责所需能力"（报告 764） | `pkg/tools/tools.go:1000-1838`（`GetPrimaryExecutor`、`GetPentesterExecutor`、`GetSearcherExecutor`、`GetReporterExecutor` 等）显式列出 `definitions`/`handlers` | 方案的通用"能力注册"可以先从现有注册表加条目开始，且只加在允许使用该能力的角色执行器里 |
| 2 | `Functions.Disabled` + `Functions.Function`（带 JSON Schema 的外部 HTTP 能力）已打通 REST/数据库/payload——但未被使用 | `pkg/tools/tools.go:39-68`、`pkg/server/models/flows.go:57`、`pkg/controller/flow.go:432-441`，仅在 `tools.go:443-444` 赋值 | legacy 通路隔离（阻塞项二）与 Skill/MCP 适配路径（§3.4）。接上它成本很小，另建一套能力系统成本很大 |
| 3 | 每个工具调用只有一个分发点并已写入 `toolcalls`；每个容器执行只有一个咽喉点 | `pkg/tools/executor.go:242`（`Execute`）、`:289`（`tclp.PutLog`）；`pkg/docker/client.go:907`（`ContainerExecCreate`，仅 3 个调用方） | 报告的控制面检查（675-677）与审计轨迹。**两个点都要覆盖**——只在 `executor.go` 设卡会被下一个直接 `docker exec` 绕过 |
| 4 | 内容寻址的资源/流程文件存储 | `pkg/flowfiles/files.go:565`（`Hash … // MD5 hex` 作为 blob 名）、`:609` blob 解析 | 证据原文存储。可直接复用，但要加 SHA-256：方案与报告都要求完整性校验（报告 715；方案 §3.1），MD5 撑不起这个说法 |
| 5 | 持久生命周期作业：单流程 FIFO、跨流程 4 并发、重试、重启恢复 | `pkg/controller/flowjobs.go`（`flowJobRunner`）、迁移 `20260913_180000_flow_jobs.sql`、`flowjobs_test.go` | 新的阶段/验证调度器。改为按阶段扩展 `flow_jobs.kind`；并定义五阶段任务的阶段作业如何排队 |
| 6 | GraphQL + 订阅 + Apollo 代码生成 + 流程/消息/终端/工具调用订阅 | `backend/pkg/graph/schema.graphqls`（1204 行）、`frontend/src/graphql`、`frontend/src/providers` | 多页面 UI 的实时状态。方案从未决定新对象走 REST 还是 GraphQL——建议读/订阅走 GraphQL，导入/导出走 REST，并写进方案 |
| 7 | 集中式中文文案表 + 参数化键 | `frontend/src/locales/zh-CN.ts` + `uiText(...)`；方法论见 `IMPLEMENTATION_STATUS.md:262-269` | §P0A 第 2 条说要"建立统一中文术语和集中维护的文案机制"——它已经存在；要求新页面必须使用它，并把方案自己提的"持续门禁"落成脚本/测试 |
| 8 | 报告链路：提示词模板、后端报告装配、前端报告页与 MD/PDF 导出 | `backend/pkg/templates/prompts/reporter.tmpl`、`backend/pkg/reports/assistant_report.go`、`frontend/src/pages/flows/flow-report.tsx`、`frontend/src/lib/report/report.ts` + `report-pdf.tsx` | P5 的中文报告。在模板与装配上加证据驱动字段即可，不要另建第二套报告引擎 |
| 9 | 知识文档 + pgvector 检索，已按 `flow_id` 过滤，共用单一集合 | `pkg/tools/memory.go:67`（flow_id 过滤）、`pkg/tools/tools.go:429`（`WithCollectionName("langchain")`） | 漏洞收集阶段的文档库与 RAG。注意细节：跨**任务**泄漏已经防住，跨**项目**没有，需要额外元数据过滤或按项目建集合——这是代码改动，不是配置 |
| 10 | 每流程 Docker 容器 + 终端/文件/截图/文件拉取回传 | `pkg/docker`、`pkg/tools/terminal.go`、`pkg/flowfiles` | 原始证据采集（输出、截图、拉取文件）。复用，并加 SHA-256 与不可变包装 |
| 11 | 交互助手模式 + `AskUser` 屏障 | `pkg/providers/assistant.go`、`pkg/tools` 屏障工具 | 人在环路。R2/R3 审批建议用结构化 `Approval` 记录而不是聊天路径，但可复用其 UI 与通知挂钩 |
| 12 | 可观测性：OpenTelemetry + Langfuse 追踪模型与工具调用 | `pkg/observability`、`pkg/observability/lfclient.go` | 报告要求的结构化智能体事件时间线（765）；同时遵守 765"审计内容不用完整思维链"的要求，据此调整保留策略与详细度 |

**如果照此调整**：P2 从"建策略网关和工具运行器"变成"接白名单、加分发点装饰器、限制容器网络、补测试"；P4 保留真正新增的确定性搜索；P5 变成"模板 + 证据装配"而不是新建报告子系统。

一处需要提醒的反向事实：PentAGI 目前**没有任何依赖/DAG 基座**，也没有仓库内 `WITH RECURSIVE` 先例（全仓库 0 处），所以"用关系表做有界图搜索"在本仓库属于未验证工作，建议 P4 早期先做一个计时探针。

---

## 5. 风险清单

| 编号 | 风险 | 为什么重要 | 建议 |
|---|---|---|---|
| R-1 | 阶段↔Flow 映射未定义（§3.1） | 界面与执行各说各话，"真实产物"门禁变成摆设 | G1 出映射表；先于 P1 迁移冻结 |
| R-2 | 证据完整性建立在 MD5 存储上 | 方案与报告要求 SHA-256 完整性；`flowfiles` 用 MD5 命名 blob | 证据入库时计算 SHA-256；资源库保留 MD5；两者都存 |
| R-3 | legacy 隔离未实现（§3.2） | P2 出口条件无法达成；报告"授权越界 = 0"无法证明 | 接 `Functions.Disabled` + 分发点装饰器 + 负向测试 |
| R-4 | R3 网络隔离被默认成立 | 所有流程容器共用一个全局 `DOCKER_NETWORK`（或 host），现状无法落实 R3 | G2 定案：按任务建 Docker 网络（现有创建路径已支持具名网络）、代理白名单、或独立 runner |
| R-5 | 审批人身份规则缺失 | R3 要求两名不同审批者；现只有 admin/user 两角色（roles/privileges 结构可扩展） | P2 增加审批人角色/权限 + "审批人≠申请人""两名不同审批人"约束 |
| R-6 | 版本比较器是正确性关键路径 | SemVer/dpkg/rpm 的 epoch、revision、预发布等边界是误报的经典来源 | 使用成熟库；`dpkg`/`rpm` 只在夹具中调用；保留 `unknown` 语义；冻结源自 NVD 的夹具（报告正是这么要求的） |
| R-7 | 工具输出/抓取页面引发提示注入（§3.8） | 报告 677 禁止内容改变策略/状态或触发调用 | 来源标记 + 范围重校验 + 对抗性夹具 |
| R-8 | outbox + pgvector + 可选图谱集中在单机 | 16GB 开发机上有三处一致性面，故障会表现为"索引落后" | 首个切片在投影降级时仍可用；界面显示积压与最后错误（报告 784 的数据状态） |
| R-9 | 靶场与夹具排得太晚 | P3/P4/P5 的 golden test 与 F2 的真值集都需要冻结环境（报告 796：GNS3/Vulhub/Juice Shop/DVWA/Metasploitable） | P1 就启动靶场；P3 前冻结真值 |
| R-10 | 按决定不做备份（`DEPLOYMENT.md`） | schema 出错不可恢复 | 落实方案自己 P1 要求的前后向演练，并按切片导出 schema 备份；用种子夹具替代已清空的流程数据 |
| R-11 | "16GB" 并发预算只是定性描述 | Nmap + 无头浏览器 + LLM + Postgres 会互相挤压 | 按项目/安全域设定显式默认上限（报告 369/452 本要求并发可配置），并在 P2 实测 |
| R-12 | 报告没有数值阈值 | 除 15%、R0–R4、三态/七态表、八模块外，所有深度与阈值都写明"可配置、实验前冻结"（608、809） | 方案的默认值作为**带版本的默认值**可以，但要在配置表里写明它们是记录在案的场景参数而非常量 |
| R-13 | 没有依赖/DAG 基座，且 PentAGI 的 subtask id 不稳定 | `subtasks` 无边；执行是顺序 FIFO（`pkg/controller/task.go:302-329`）；`RefineSubtasks` 删除并重建计划行（`pkg/controller/subtasks.go:97-139`），以 `subtask_id` 为键的边表会失效 | 验证任务图完全放在新业务表、用自有稳定 ID；把就绪集调度与环检测列为 P4 显式工作；不要把依赖映射到 PentAGI subtask |
| R-14 | 新增业务智能体角色的成本高于方案预期 | 一个角色要改 `pconfig.ProviderOptionsType`、GraphQL `AgentType`、DB 枚举 `MSGCHAIN_TYPE`（迁移）、提示词、以及设置页/`AgentConfigType` | P4 的利用链角色按跨切面改动估算；若契约允许，优先做成现有角色的提示词+工具权限变体 |
| R-15 | 报告快照与服务端导出是全新工作（§3.9） | 现在报告是可变的行，PDF 只在浏览器生成，无发布/证据绑定 | P5 规划期决定浏览器端还是服务端导出；快照 schema 要支持断模型重建 |
| R-16 | 代码生成与 CI 门禁 | schema 改动需要 gqlgen + `sqlc generate` + `swag` + `pnpm run graphql:generate`；前端 CI 会因 `types.ts` 过期而硬失败，而**后端 CI 的 `go test ./...` 与 golangci-lint 是 `continue-on-error`** | 把代码生成与后端门禁写进每个切片的出口条件；估算时计入代码生成时间 |
| R-17 | 订阅只在进程内 | GraphQL 订阅是单进程 channel 扇出（`pkg/graph/subscriptions/controller.go`） | 当前单机部署可接受；不要按多副本设计实时状态 |

---

## 6. 排期与工作量合理性

- **按现有文字，P1 实际是两个切片。** 数据模型 + 范围隔离 + 导入 + 归并引擎 + 证据侧栏 + 五阶段路由 + 共享上下文 + outbox，超过一个迭代。建议拆分：
  - **P1a** —— schema + `Project/Scope/Authorization` + 资产/观测 + 证据（SHA-256）+ Nmap/CSV 导入（校验预览→确认提交）+ 归并服务 + 项目范围强制测试。
  - **P1b** —— 任务中心 + 五阶段骨架 + 共享上下文 + 证据侧栏 + 跨页跳转，读取 P1a 的真实数据。
- **先做一条走通的端到端骨架。** 铺开之前先验证一条细路径：建项目 → 导入 Nmap XML → 1 个资产 → 1 条候选漏洞 → 1 条手工候选链 → 1 次经网关的 R1 只读核验 → 1 条证据 → 1 个中文报告页。用最低成本验证阶段↔Flow 映射、网关、证据 schema 与界面接线。
- **靶场提前**（P1），真值集在 P3 前冻结。
- **关键路径**：G1（ID、状态、范围、阶段映射）→ 导入/归并 → 证据 → 网关 → 低风险能力 → 候选匹配 → 有界搜索 → 验证任务图 → 证据驱动报告。
- **暂不出工期**是可以的（方案把估算挂在 G1 上是对的），但要写明每个切片的估算口径与评审节奏，否则"不出工期"会变成"没有交付约束"。

---

## 7. 方案文字中的事实性更正

1. **访问协议。** 方案说入口是 `https://localhost:8443`（第 5、9、383 行）。自 2026-09-14 起部署为 HTTP：`SERVER_USE_SSL=false`，`http://localhost:8443`（`DEPLOYMENT.md:3, 24, 34`）。请更正基线段落。
2. **角色数量。** "现有 13 个模型角色"对可配置的按角色模型配置是准确的（`pkg/providers/pconfig/config.go:141-153`），但 `AgentType` 枚举有 15 个值（`pkg/graph/model/models_gen.go:734-769`）。建议写成"13 个可配置模型角色 / 15 个 Agent 类型"。
3. **P0 回归基线。** 第 246 行要求归档现有回归数据与迁移版本。数据库已于 2026-09-14 按用户要求清空（`flows` 0 行，`DEPLOYMENT.md:27`），没有流程数据可归档；基线是代码 + 测试 + 一个新的种子夹具。请改写。
4. **向量检索隔离。** §2.1 写"避免检索串任务或串项目"。跨**任务**检索已按 `flow_id` 过滤（`pkg/tools/memory.go:67`），缺的只是跨**项目**范围。措辞精确与否直接决定改动量大小。
5. **适配器接口重复。** §3.5 的 `Describe/Validate/Plan/Execute/Cancel/Parse/Health` 与现有 `FunctionDefinition` + `ExecutorHandler` + `IsAvailable` + JSON Schema 重叠。请做映射，而不是引入平行概念。
6. **阶段编号。** `IMPLEMENTATION_STATUS.md:280` 仍用旧方案的"P1–P6"，新方案用 P1–P5。加一行新旧对应，避免后续状态记录混乱（状态文档也要同步）。
7. **工具运行器。** 方案把独立 runner 进程当作新组件。G2 前先确认现有 worker/作业模型能否承载，再决定是否新增服务与 Compose 条目；方案自己的资源说明也支持在本机保持克制。
8. **中文化机制。** §P0A 第 2 条把"建立统一中文术语和集中维护的文案机制"列为待办；它已存在（`frontend/src/locales/zh-CN.ts`、`uiText`）。改为"扩展现有机制并加门禁"。另外"全站中文化"对**数字**格式并不成立（`frontend/src/lib/utils/format.ts:22` 使用 `Intl.NumberFormat('en-US')`，`formatDuration` 直接输出 `h/m/s`）；请把数字与时长纳入 P0A/P1 范围，并记住新页面要补侧栏入口、面包屑与 `frontend/src/lib/route-titles/index.ts` 条目。
9. **P0 收口条件。** 第 246 行应要求附新产物：方案引用的 p95 数值与 2026-09-14 之后的测试总数在 `build/` 下不存在，真实 Postgres 作业套件在没有 `PENTAGI_TEST_PG_DSN` 时会静默跳过（`pkg/database/flowjobs_pg_integration_test.go:28-30`）。"以重新运行日志关闭 P0"是一行改动，却消除了整条状态链上的可信度风险。

---

## 8. 建议对方案做的具体修改（可执行清单）

1. 新增 **§2.3 研究对象 ↔ PentAGI 执行对象映射**（§3.1 的表），并列为 G1 冻结项。
2. **P2** 增加：把 `Functions.Disabled`/白名单接入执行器构建；在 `customExecutor.Execute` 加分发点装饰器；同时覆盖 `ContainerExecCreate` 咽喉点；限制研究流程容器网络；补三条负向测试。
3. **G1** 增加：状态词表映射表（报告 ↔ 方案 ↔ 界面 ↔ 数据库）与跨项目范围强制测试。
4. 增加 **"范围外但保留接口"** 小节：活动/工时埋点、真值与证据门槛标记、带版本的场景参数；并就报告研究内容 2 的机器学习部分取得书面决定。
5. **P2** 增加**审批队列页面 + 审批人身份/角色规则**；**审计写入点**从 P1 开始，P2 给出只读审计页。
6. §3.5 增加 **Skill/MCP 映射**，并说明表 5.1 八个组件哪些复用、哪些新建。
7. §3.7 增加**提示注入 / 外部内容即数据**的控制与测试。
8. §7 增加**报告八模块 ↔ 首版覆盖矩阵**（含五个权限域），明确列出延后项。
9. P4/P5 增加 **SHA-256 证据哈希**与**图谱投影的伴随决定**（§3.7）。
10. **P1 拆成 P1a/P1b**，加入走通的端到端骨架，并把**靶场搭建 + 真值冻结提前到 P1**。
11. P5 增加**报告快照与发布门禁设计**（以及浏览器端/服务端导出的选择），不要留到实现期再定（§3.9）。
12. 每个切片的出口条件加入**代码生成与门禁步骤**（gqlgen / `sqlc generate` / `swag` / `pnpm run graphql:generate`，以及后端测试门禁）；注意 P4 新角色会牵动五处代码加一个数据库枚举迁移。
13. 明确 **subtask id 不稳定**这一约束：验证任务图不得以 PentAGI `subtask_id` 为键（§3.1、R-13）。
14. 中文化范围补入**数字与日期格式**，并登记五个新页面的路由/导航/面包屑。
15. 更正 §7 中列出的事实性问题。

---

## 9. 会改变结论的几个问题

1. **验收范围。** 结题/验收是否要求报告第 8 章实验、两项 15% 指标，以及研究内容 2 的机器学习部分（资产分类、历史预测、误报过滤）？如果是，方案的延后需要现在就补埋点（§3.3）或取得书面偏差确认；如果不是，方案可维持现状，我只保留埋点建议。
2. **截止时间。** 合同/结题日期，或系统必须可演示的日期？这决定是否要重排切片、把可端到端演示的路径前置。
3. **研究成果。** 报告"科技研究成果专页"（专利/软著/论文）是空的（报告 23-29）。若要出软著或论文，知识图谱/MCP/机器学习的说法就需要可演示的对应物，这会提高 §3.4 与 §3.7 的优先级。
4. **靶场承载。** 隔离靶场（Vulhub/Juice Shop/DVWA/Metasploitable，可选 GNS3）能否跑在这台 16GB 机器上，还是需要独立机器/虚机？这决定 P1 夹具方案与并发预算。
5. **运行模式确认。** 单套部署、HTTP `localhost:8443`、不做备份、数据库已清空——若确认，方案应把"种子夹具 + schema 导出 + `goose down` 演练"写成替代备份的恢复手段。

---

## 附录 —— 核验记录

- P0 代码层结论已对源码与测试核验（迁移 `20260913_180000_flow_jobs.sql`、`pkg/controller/flowjobs.go`、12 个生命周期/并发测试）。方案引用的 p95 数值与 2026-09-14 之后的部分测试总数**无法核验**——`build/` 下无产物。
- 中文化已核验为真实机制（`frontend/src/locales/zh-CN.ts`、`uiText`，约 1,498 处调用 / 154 个文件），残留英文的分类记录见 `IMPLEMENTATION_STATUS.md:277`。
- `Functions.Disabled`/`Function` 未被消费已通过全 `backend/` grep 核验（仅 `pkg/tools/tools.go:443-444` 赋值，以及 `cmd/ftester` 一处使用）。
- 工具分发单点核验于 `pkg/tools/executor.go:242`，工具调用日志在 `:289`；容器执行咽喉点在 `pkg/docker/client.go:907`（3 个调用方）。
- 全仓库无依赖边：`subtasks` 表结构与顺序执行循环（`pkg/controller/task.go:302-329`）；`RefineSubtasks` 重建计划行（`pkg/controller/subtasks.go:97-139`）。
- 内容寻址文件存储核验于 `pkg/flowfiles/files.go:565, 609`（MD5 hex blob 名，路径哈希在 `:193-196`）。
- 报告可变性核验：`pkg/controller/task.go:359-368`（msglog）与 `pkg/reports/assistant_report.go:334-351`（原地覆盖）；PDF 在客户端（`frontend/src/lib/report/report-pdf.tsx`）。
- 全新字段确认：资产／服务／组件／漏洞／发现／证据／审批／项目／范围／候选链／验证任务在现有 26 张表中均不存在（`pkg/database/models.go`）；全仓库 `WITH RECURSIVE` 为 0 处。
- 向量检索的流程隔离核验于 `pkg/tools/memory.go:67`；共享集合名在 `pkg/tools/tools.go:429`。
- 报告行号引用依据 `tmp/midterm-report-extracted.md`；完整需求清单见 `tmp/requirements-checklist.md`。
